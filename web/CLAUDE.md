# web/ - React 前端

> 根层概览、组件清单、token/复用规范见仓库根 `AGENTS.md`。后端协议/端口/CLI 见 `src-tauri/CLAUDE.md`。跨平台 CI / smoke 矩阵见 [`docs/development/testing.md`](../docs/development/testing.md)。

交互式终端输入固定走 `cc-partner.terminal-input.v1` 常驻 WebSocket：桌面 invoke 只等待 GUI Rust 有界队列接纳，mobile 连接同源 `/api/mobile/workbench/terminal-input-stream`，peer 数据阶段不得逐帧 health/HTTP。ACK 仅确认 PTY write+flush，不作为下一帧发送闸门；断线未 ACK 输入不得自动重放，也不得回退 `/sessions/write`。输出继续使用 NDJSON/replay/gap。

## 概述

基于 React + TypeScript + Vite 的前端界面，宿主为 **Tauri 2**，桌面主界面通过 `invoke()` IPC 调用 Rust 后端命令（`src-tauri/`）。`/mobile` 普通浏览器 SPA 通过同源 HTTP 调用后端移动端/Workbench routes。

## 开发命令

- `npm run dev` — 启动 Vite 开发服务器（端口 5173）。**移动端热更新**：debug 后端会把扫码入口 `http://<LAN>:<实际端口>/mobile` 反代到本 Vite（含 HMR WebSocket）；改 `src/mobile/**` 不必 `npm run build`。详见 `docs/development/backend-operations.md`「Mobile `/mobile` hot reload」。环境变量 `CC_PARTNER_MOBILE_DEV_PROXY` / `CC_PARTNER_VITE_DEV_URL`。
- `npm run build` — 打包到 dist/（`tsc -b` 类型检查 + vite 构建；类型检查入口，**不要**用浮动 `npx tsc`）
- `npm run lint` — ESLint 检查
- `npm run check:css-tokens` / `npm run check:tokens` — design token 合同（权威实现 `web/scripts/check-css-tokens.mjs`；根 `scripts/check-css-tokens.mjs` 仅为 thin wrapper）：扫描 `src/**/*.css` 的 `var(--name)`，语义 token 必须在 `styles/tokens.css` 的 `:root` 定义；颜色/阴影类 token 必须浅/深双份；正文语义色对（`fg`/`fg-2`/`muted`/`fg-muted-readable` × `bg`/`surface`）浅/深均 ≥4.5:1；`color: var(--meta)` 仅允许脚本内 `META_COLOR_ALLOWLIST` 的 disabled/decorative/placeholder；运行时结构变量 allowlist 含 `--prompt-panel-left` / `--prompt-panel-top` / `--git-graph-color` 与 mobile shell `--mobile-shell-height` / `--mobile-keyboard-inset` / `--mobile-terminal-min-height`；通过打印 `CSS token contract passed`（exit 0），失败打印 `file:line --token` 或 contrast/meta 诊断（exit 1）。CI quality 在 build 前执行（`check:tokens`）。fixture：`node --test scripts/check-css-tokens.test.mjs`；CLI `node scripts/check-css-tokens.mjs --self-test` 等价。
- `npm run check:bundle` — 拆包合同（权威实现 `web/scripts/check-bundle-contract.mjs`；根 `scripts/check-bundle-budget.mjs` 仅为 thin wrapper）：读取 Vite 写出的 `dist/.vite/cc-bundle-contract.json`，对 `main`/`mobile` entry 沿静态 `imports`（排除 `dynamicImports`）求 JS 闭包 gzip，并加上入口 HTML 直接引用的 stylesheet（`entryStyles` / dist `index.html`·`mobile.html` 的 `<link rel="stylesheet">`）gzip；**最终硬顶**（baseline 不得抬高）：desktop main ≤ 320 KiB、mobile initial ≤ 280 KiB、单 lazy JS chunk ≤ 700 KiB、全部 runtime JS gzip ≤ 1,400 KiB、dist `*.map` raw 合计 ≤ 2 MiB 或无 map。可选 baseline ratchet（`web/scripts/bundle-budget-baseline.json` 与根 `scripts/bundle-budget-baseline.json`）：默认 **strict** 对各指标取 `min(baseline, final)` 禁止增长；**禁止手改 baseline 上抬**（`--write-baseline` 仅 `min(previous,current)`）。本地 toolchain/env 漂移主机可显式 `CC_PARTNER_BUNDLE_RATCHET=final-only` 仅用最终硬顶并跳过 editor-entry baseline（CI 默认 strict，不得依赖 final-only 放行回归）。失败诊断打印 actual/baseline/final/ceiling 与 top 5 chunks，并提示 final-only 策略。报告分项 `js=`/`css=`；mobile initial 禁止 `@xterm/*`、`@tiptap/*`、`@uiw/react-codemirror`、`codemirror`/`@codemirror`、`recharts`。通过打印 `Bundle contract passed`。CI quality 在 build 后执行。fixture：`node --test scripts/check-bundle-contract.test.mjs`；CLI `--self-test` / `--write-baseline`。生产 `sourcemap` 默认 `false`；仅 `CC_PARTNER_SOURCEMAP=1` 生成 `hidden` map（无 `sourceMappingURL`）。重型边界：`WorkbenchFileWorkspace` 动态加载 Code/Markdown/HTML 编辑器；`MobileWorkbench` 动态加载 terminal/files/automation/browser/git/worktrees/prompt/settings 面板（Projects/Attention shell 同步）；Health 路由 lazy 且 StatsChart/recharts 不得进入 eager App 图
- `npm run check:i18n` — 生产 TSX i18n 合同（`scripts/check-i18n-jsx.mjs`）：typescript AST 扫描 `src/**/*.tsx` 的 JSXText 与 `title/aria-label/placeholder/alt` 字符串字面量；含中文/拉丁字母的文案必须来自 `t()` 或有限品牌 allowlist（`cc-partner`/`Claude Code`/`Git`/`GitHub`/`HTML`/`JSON`/`SQLite`/`tmux`/`WSL` 与纯标点）；排除 `.test`/`.stories`/`DesignSystem`/`testing`；诊断格式 `file:line:column`。CI quality 在 build 前执行。fixture：`node --test scripts/check-i18n-jsx.test.mjs`；zh/en key 对等：`npm test -- localeParity`
- `npm test`（`vitest run`）— 单次跑全部单元测试；Vitest 按 `vitest.config.ts` 的 `include: ['src/**/*.test.{ts,tsx}', 'tests/support/**/*.test.ts']` 自动收集（E2E `*.spec.ts` 排除）。按路径过滤：`npm test -- src/pages/Workbench` 或 `npm test -- attention`；harness 合同：`npm test -- tests/support/backendHarness`；故障分类与恢复策略：`npm test -- src/lib/faultRecovery`
- `npm run test:unit:watch`（`vitest`）— 单元测试 watch 模式
- `npm run test:e2e`（`playwright test`）— 前端 E2E；配置在 `playwright.config.ts`（单 project `chromium`，`outputDir: test-results`，失败保留 screenshot/trace/video，CI `retries: 1`；mobile 仅用 per-test viewport，不另开 browser project）。用例从 `tests/fixtures.ts` 导入 `test`/`expect`：auto fixture 监听 `console.error` 与 `pageerror`，意外浏览器错误即失败；opt-in fixture `backendHarness`（`tests/support/backendHarness.ts`）经 `page.addInitScript` 注入确定性 `__TAURI_INTERNALS__.invoke` / 同源 `/api/*` fetch / event listen，支持 `resolve|reject|defer|fault`、路径模板 `/api/.../:id`、调用序列与 `assertSettled`（pending/未消费预期/泄漏 listener）；失败附加 `harness-calls`；**生产代码不得读取测试全局**。既有 ad-hoc mock（attention/core-integrity 等）仍可用、不强制迁 harness。核心产品完整性旅程：`npm run test:e2e -- core-integrity.spec.ts`（Transfer send/cancel、Prompt reject rollback、Welcome 权限失败重试；精确 mock `list_devices`/`list_transfers`/`send_transfer`/`cancel_transfer`/Prompt/permission 命令与 `plugin:dialog|open`，无生产 bypass）。S6 关键桌面数据旅程（backendHarness + `tests/support/appBootstrap.ts`）：`npm run test:e2e -- transfer.spec.ts scratchpad.spec.ts prompts.spec.ts permissions.spec.ts settings.spec.ts`（稳定 ID：`E2E-TRANSFER-001` / `E2E-SCRATCH-001` / `E2E-PROMPTS-001` / `E2E-PERM-001` / `E2E-SETTINGS-001`）。S6 Workbench/Mobile/LAN 边界旅程：`npm run test:e2e -- workbench.spec.ts mobile-workbench.spec.ts lan-boundary.spec.ts`（`E2E-WORKBENCH-001` 项目 stale/offline/文件/listener 基线；`E2E-MOBILE-001` 390×844 导航/Drawer/HTTP 写/stale；`E2E-LAN-001` L1 无凭据业务 + fault 模拟边界拒绝，真实多机 ConnectInfo/**NOT VERIFIED** 见 L2 `L2-LAN-TRUST-BOUNDARY-001` / `lan_trust_boundary_smoke`）。N6 Orchestrator Human Review / WORKFLOW / 运营通知旅程：`npm run test:e2e -- orchestrator-review-workflow.spec.ts`（稳定 ID `E2E-ORCHESTRATOR-REVIEW-001`：review diff → Rework、digest 漂移 Deliver conflict、WORKFLOW invalid→fix→valid CAS、运营通知 privacy/dedupe、Attention deep link 只导航不执行业务动作）。**Agent Hub Gate A/B/C/D**：`npm run test:e2e -- agent-hub.spec.ts`（稳定 ID `E2E-AGENT-HUB-A-001` + `E2E-AGENT-HUB-B-001` + `E2E-AGENT-HUB-C-001` + `E2E-AGENT-HUB-D-001` + `E2E-AGENT-HUB-PORTABLE-001`：status card / 项目 preview+enable / target matrix / aggregateStatus / conflict deep link `assetId`+`conflictId` / 旧路由 `/claude-md`→`/agent-hub`、`/claude-code`→`/agent-hub?section=assets&target=claude`；portable 3×4 inventory/filters/actions/same-agent Pull / scope·kind 过滤 / invocation alias / externalCollision adoption 预览 / detached restore·remove / target enable / delete everywhere / LAN source-push 选择与 per-target progress（含 unsupported）/ Git lane inspect·preview·confirm / credential 披露 / stale preview / project mapping / Plugin 组件 Drawer + ownership delete preview + residual / OpenCode provider catalog fail-closed + bridge preview deep link；mock `agent_hub_*` + `list_devices` + `list_orchestrator_agent_adapters`；不宣称真实多 CLI 写盘、marketplace 激活、真实 OpenCode TUI、双主机 mDNS 或自动 Git import）。**Program Task 5（2026-07-30 integration）**：E2E 8 例 + 聚焦 unit（AgentHub/attention/typeBarrel/localeParity）+ build 已在集成树复跑通过；`npm run lint` 仍有 Agent Hub 侧 React Compiler/hooks 债务（不得 false-green）；`check:bundle` 默认 strict baseline 可能因本地 toolchain 漂移失败，CI 保持 strict，本地仅 `CC_PARTNER_BUNDLE_RATCHET=final-only` 用最终硬顶。稳定 evidence ID 权威登记：`docs/development/quality-matrix.json`（`node scripts/check-quality-traceability.mjs`）；L1 mock 不得宣称 GUI/系统权限/双机 LAN
- `npm run test:all` — 串行 `npm test` + `npm run test:e2e`，等价于一次本地前端 CI
- `npm run test:e2e -- frontend-foundation.spec.ts` — S4 基础合同冒烟：Dialog 焦点环/恢复、窄屏 mobile Drawer Escape、Attention 单 tab stop、终端 tab 方向键、DEV 路由崩溃夹具恢复、`prefers-reduced-motion` 归零；bundle 合同仍走 `check:bundle` 而非 E2E 计时
- 完整开发（前端 + Rust）：仓库根 `./start.sh` 或 `./web/node_modules/.bin/tauri dev`

### 测试环境约定

- Vitest 默认 **Node environment**（`vitest.config.ts` 中 `test.environment: 'node'`），不全局启用 jsdom
- 只有真正访问 DOM 的测试文件，才在文件顶部显式声明 `// @vitest-environment jsdom`
- CI 只用 `npm ci` 安装锁定依赖；**禁止** 带 `--yes` 的浮动 `npx` runner，也**禁止**文档/脚本推荐按文件手敲的 `npx` + `tsx` 单文件测试入口——一律走 `npm test` / `npm run test:e2e`
- 稳定命令四条：`test` / `test:unit:watch` / `test:e2e` / `test:all`（与 `package.json` 一致）

### 前端约束摘要（本目录必守）

- **组件 / token / i18n**：颜色间距圆角阴影 100% `tokens.css`；禁止未定义语义 token 或为错误名加 alias（如 `--bg-2`→`--bg`、`--fg-muted`→`--muted`/`--meta`、`--warning`→`--warn`）；用户可读的小字/次要信息用 `--fg-muted-readable`（≥4.5:1），禁止把 `--meta` 当正文；`--meta` 只用于 placeholder/disabled/装饰并需 allowlist；`globals.css` 在 `prefers-reduced-motion: reduce` 下对 animation/transition duration **有意使用 `!important` 全局归零**，确保组件级 transition 无法重新引入动效；产品接受「无例外」a11y 语义；用户可见文案走 `src/i18n`；详见根 `AGENTS.md` §3–§5 与本文 i18n 节
- **Hooks 顺序**：所有 hooks 与 Workbench controllers 必须在 early return 之前（根规则 + `AGENTS.md` §5.8）
- **巨型页 controller/view 拆分**：AgentHub → `useAgentHubController` + `AgentHubView` / `InstructionBlocksDrawer` / `TargetStatusCell` / `AssetAdoptionDialog` pure views（views 禁止 `@/api/*`；refreshSeq/detailSeq/scopeCursor 防 stale 写入；target-local enable/presence/restore/delete-everywhere）；Settings → `useSettingsController` + General/Sync/Dependencies/AI/About pure panels（views 无 API；另有既有 Health/Automation panels）；桌面 Orchestrator → `useOrchestratorController` + Board/Outbox/TaskDrawer/CreateDialog views；Mobile Automation → `useMobileAutomationController` + TaskList/TaskDetail/CreateDialog/Outbox；禁止第八个 Workbench 聚合 controller；`Workbench.tsx` ≤1200 行；手动 a11y smoke 建议 VoiceOver/NVDA 覆盖 Dialog/Drawer/Attention 行/终端 tabs。Settings controller 由 `useSettingsController` composer + `controllers/useSettingsResources` / `useSettingsFormSaves` / `useSettingsUpdatePermissions` 三域 hook 组成；桌面 Orchestrator controller / mobile automation controller 仍是大型编排模块（结构债，非行为 bug）。
- **Agent Hub 交互重设计（2026-08-10）**：权威 IA 为 **Tab → 范围 →（提示词：三槽 lane）→ Agent**（`context/agentHubContext.ts` parse/write 含 `instructionLane`；`shell/AgentHubShell.tsx` pure chrome）。提示词主路径 `instructions/`（`InstructionThreePaneView` + `useInstructionThreePaneController`）：canonical 固定 **公共 shared / 适配 adapted / 独有 targetOnly** 三槽（每 mode 至多 1；`normalizeInstructionBlocks` / 后端 `normalize_to_three_slots`）；打开 hydrate 时旧 N 块按 mode 归并。**按 lane 布局**：壳层 lane 顺序 **独有 → 适配 → 公共**；公共 → 仅公共槽单列（无预览/原始、**隐藏 agent 导航**，不展示重新扫描/写入原始）；适配 → 当前 agent 单列编辑 `adapted.variants[agent]`，工具栏「保存三槽」旁「适配到其他 Agent」调用 Claude 改写并**追加**到其他 agent 适配槽尾（壳层工具栏不再提供同名按钮）；独有 → 当前槽/合成预览/原始文件三列，原始栏「分析拆解」调用 Claude 把原文拆成公共+适配+独有并**覆盖**现有三槽（不再提供「从原始导入为公共」）。壳层 lane 选择当前槽编辑（公共写 shared.common；适配写 adapted.variants[agent]；独有写 targetOnly.variants[agent]）；合成预览顺序 公共→适配→独有；「保存三槽」`saveBlocks` 独立持久化 canonical head。**InstructionBlocksDrawer**（legacy matrix 入口）同样固定三槽编辑，不再 promote/pair 多块。portable 发现即管理、无 Adopt 主 CTA；冷启动用 `agent/tab` 灌 filters（无 legacy target 时默认当前 Agent）；资产 `policy` 文案对齐公共/适配/独有。跨 Agent：`crossAgent/CrossAgentAdaptPage`（`view=adapt` 全页；selective 文案说明三槽投影；full 强制 preview）。LAN 拉/推走壳层工具栏。单元：`npm test -- agentHub AgentHubShell instructionThreePane crossAgent portableInventoryPresentation useInstructionThreePane useCrossAgentAdapt usePortablePull useAgentHubController localeParity`；L1 mock：`npm run test:e2e -- agent-hub-interaction.spec.ts`。旧 Gate A–D portable 旅程仍在 `agent-hub.spec.ts`。
- **Agent Hub 用户级指令 V2**：`pages/AgentHub/userInstructions/` 保留并复用逐目标 preview Dialog；主编辑面为三槽。workspace 以严格 decoder 读取 `canonical`、真实 source/path、capability/evidence、`inventorySnapshotHash` 与 `contentTruncated`。旧后端只允许 inspect 的显式只读 fallback，任何 V2 preview/apply 命令缺失都必须报 `USER_INSTRUCTION_V2_BACKEND_UNAVAILABLE`，不能回退到旧写路径。可读的本机用户级目标开放“逐文件 diff → 用户确认 → expected-hash + AtomicProjectionWriter”人工写入；support manifest 无 L3 时仍阻断后台自动投影、portable mutation 与删除。Hooks 全部位于早退前。测试：`npm test -- useUserInstructionManager instructionThreePane`；浏览器 mock 以三栏旅程为主（`E2E-AGENT-HUB-INSTR-3PANE-001`）。
- **Agent Hub 安全纠正（2026-08-10，覆盖上方旧 Gate A–D UI 描述）**：生产 context 固定 local-user；Shell 是 Agent/lane/Tab 唯一真源，project/peer 直接管理 fail-closed。三栏以 committed context + draft lease 隔离，dirty refresh 只更新 observed snapshot，Canonical/source drift 分开，Save 只消费 blocks dirty。跨 Agent 仅 local-user selective preview，apply/full 在 API 前拒绝；portable inventory 是唯一资产 UI，legacy matrix/writer/Git Import drawer 不进生产 DOM；旧 URL 仅迁移到 `agent/tab/inventoryItemId`。Pull refresh 失败保留 stale inventory 但清 plan/禁 Apply，危险选项按 dialog session 恢复安全默认。主 Tab 用 tablist；scope/lane/agent 用 radiogroup；库存选择按钮与行内动作组是兄弟原生按钮。portable inventory 行内直接渲染 capability 驱动的多动作组（`resolvePortableRowActions`：enable/disable 按 `actualEnabled` 互斥 + `installToSourceTarget` 仅 null 语义 + 末位 `uninstall`），与详情 Drawer 共享同一组 stale/mutationBlocked/locked/readOnly/unsupported/unmanaged 门闩；点击行内任意动作经 `openAction` 复用既有 `PortableAssetActionDialog` 预览/确认流程，行内 testid `portable-row-action-${action}-${id}` 与 drawer `portable-action-*` 区分；用户无需打开详情 Drawer 即可启停/卸载。当前聚焦回归：`npm test -- src/pages/AgentHub src/api/agentHub.crossAgent.test.ts src/lib/rovingTablist.test.ts`。
- **Workbench 页面合同**：`Workbench.tsx` ≤ **1200 行**；域逻辑在 `controllers/` 七个 hook；禁止页面直接 `workbenchApi.(sessions|files|worktrees|git)` 或 `listen('workbench:terminal-status|merge-progress')`。**启动表面（N4 Task3/Task5）**：`projectsLoading` 时中性 loading（`data-testid=workbench-projects-loading`），**禁止**把加载中的 `projects=[]` 当成真·零项目 CTA；列表就绪后：零项目 → `WorkbenchLaunchSurface mode=empty` 只渲染纯空态视图（一句解释 + 添加本机/连接远端/检查 tmux），不渲染 worktree bar、terminal tabs/pane、文件预览入口与 inspector；页面在七个 controller 之后 early return，并通过 `emptyActions` 注入 `chooseAndAddProject` / `checkDependency` 回调（远端 picker Dialog 仍由 LaunchSurface 托管）；有项目未选中 → `mode=continue` 五 section + Attention 计数；active 项目 → 既有 chrome（`data-testid=terminal-pane` / `workbench-inspector`）。`/` 仍是 Home/Trending，不放 launch。
- **Attention**：Provider 共享 snapshot（桌面优先 `list_attention_items_v2` 回落 v1；Mobile 优先 `/api/mobile/attention/v2`）；成功业务动作立即 invalidation；10s 可见轮询兜底；列表只导航不动作；Agent 条目导航 terminal，不发输入
- **Agent Runtime 投影（A2）**：`useAgentRuntime(projectId)` listener-first handshake（`workbench:agent-runtime` + snapshot）；drain 后 cursor 抬到 max(asOf, drained sequences)，residual buffer 在进 live 后再扫；纯 reducer `agentRuntimeState`；Desktop tab/status 与 Mobile terminal 只消费 selector；非第 8 个 Workbench controller。snapshot 失败时 `phase='error'`（不得永久 `pending`），暴露 `refresh` 重试；Workbench 在 error 态展示可刷新 StatusMessage
- **Runtime 显示缓存**：桌面 `useOrchestratorRuntimeSnapshot` 与移动 `mobileRuntimeSnapshotStore` **进程内 Map 彼此独立**；只缓存 remote **live** 成功；不写 localStorage/SQLite；缓存不驱动 task action / scheduler

## 架构

- **业务类型域拆分**: `src/lib/types/` 按 `core` / `settings` / `workbench` / `orchestrator` / `attention` 拆分；`src/lib/types.ts` 仅为兼容 barrel（`export * from './types/index'`），旧路径 `@/lib/types` 继续可用。域文件只允许 import upstream `./core`（或其它相对域路径），禁止回指 `../types` 或 `@/lib/types`。contract：`npm test -- typeBarrel`。
- **API 客户端**: `src/api/client.ts` — 桌面端基于 `@tauri-apps/api/core` 的 `invoke` 薄封装；Rust 后端 reject 的错误（`{error:"消息", code:"validation|not_found|..."}`）经 **export** `normalizeError` 规整为带 `message` 与稳定 category `code` 的 `Error`（R12 M2）。关键 DTO 在 IPC/HTTP 边界经 `src/lib/runtimeSchema.ts`（薄门面，实现在 `runtimeSchemaPrimitives.ts`）组合式 decoder + `src/lib/schemas/*` 域 schema **fail-closed** 校验：`invokeDecoded(command, args, decoder)` 与 decoder-aware `getJson/postJson`（可选 `decoder`，**仅成功 body** 解码；错误体不走 schema）。未知额外字段允许前向兼容，必填/枚举/有限数严格；legacy default 仅在对应 schema 字段显式声明；`ContractDecodeError` 只暴露 contract/path/actualKind，禁止序列化 payload。已覆盖 protocol health/capabilities/P2pErrorEnvelope、Attention snapshot、Orchestrator task/runtime/outbox/task-view/evidence/refresh 与 mutation task-view 路径、Transfer task/send/cancel/progress/status、Workbench project/worktree/session/path/save/fileNode/openFile（含 remote openProject、session rename、listDir/open）、AppConfig/Permissions。`/mobile` 浏览器端使用 `src/api/workbenchHttp.ts` 的同源 HTTP JSON helper 和 `src/api/mobile.ts` 的 access-info API，错误优先读取 JSON `error/message`。**Mobile HTTP transport policy**：`postJson` 必填 `HttpRequestPolicy{kind:query|mutation|longMutation|eventStream,timeoutMs?,signal?}`；query overall 默认 15s（含 body decode）、mutation 30s、longMutation 180s、eventStream 无 overall timeout；合成外部 AbortSignal + overall timeout，timeout 仅在 decode 结束后清理；query/只读 POST 对 `timeout|network` 最多重试 2 次（有界退避，仅 `document` 可见且非 callerAbort）；transport 错误只分类 `timeout/callerAbort/network/protocol/decode`。Workbench Git 四类 mutation 走 `workbenchHttp.git.{commit,push,merge,remove}`：强制非空 `clientOperationId`，返回 `WorkbenchMutationEnvelope`；仅 timeout/network 映射成功通道 `unknown`（不盲重放），protocol/decode/确定性业务错误仍抛错；`workbenchHttp.git.getMutationOperation(clientOperationId)` 查询 `/api/mobile/workbench/worktrees/mutation-operation` ledger。`MobileGitPanel`/`MobileWorktreePanel`/父级 merge 消费 envelope + `reconcileWorkbenchMutation`；unknown 进入 reconciling，pending/notFound/ambiguous 保持 unknown 并锁动作，禁止新 ID 盲重放。`httpWorkbenchTransport.worktrees.{commit,push,merge,remove}` 旧签名仍解包 envelope（succeeded 返回 value，unknown 抛中文错误）供非对账调用方。
- **故障分类与恢复**: `src/lib/faultRecovery.ts` — 纯策略模块：`classifyTransportFault` / `planFaultRecovery` 将 `networkOffline|timeout|malformedJson|permissionDenied|conflict|dbBusy|lanBoundaryRejected|crossSiteRejected`（及 Error-like `code`/`name`/`message`）映射为 typed 分类（稳定 code、retryable/showRetry、cachePolicy=`keepStale|clear|none`、错误路径 `allowOptimisticCommit:false`）；offline/timeout 保留可用缓存并标 stale，malformed fail-closed 清缓存，LAN/跨站拒绝不写 cache 且不发明 identity auth。`createTerminalFaultBridge` 提供 disconnect 归零 listener、reconnect 精确恢复 baseline、断线输入不入账与 replay 幂等。无生产 env 故障开关；L0 合同 `npm test -- src/lib/faultRecovery`。harness 导出 `FAULT_PROFILE_CODES` / `faultProfileCode` / `createFaultError` 与分类表对齐；`malformedJson` 在 invoke 上 **resolve** `{notAValidDto:true}`（生产 `invokeDecoded`→`ContractDecodeError`），fetch 仍返回非 JSON body `{not-json`。**生产接线**：`useAttention` load catch 用 `plan.keepCache` 映射 `loadFailed.hasSnapshot`；`Transfer.loadTasks` 按 `plan.clearCache` 清空/保留 tasks（错误优先稳定 code）；`isRemoteWorkbenchOfflineError` 优先 `NETWORK_OFFLINE` typed 分类，中文「远端设备不在线」仅 legacy 回退。
- **MobileAccessCard**: `src/components/domain/MobileAccessCard` 统一展示局域网 `/mobile` 访问链接和二维码，由 `AppShell` 左下角 footer 图标组（主题 / 设置齿轮 / 手机）中的手机按钮经共享 `Dialog` 模态复用；Settings dependencies tab 与 Workbench 右侧检查器不要再内嵌该卡片，也不要在页面内重复生成二维码或复制逻辑；Tauri 桌面环境通过 `get_mobile_access_info` invoke 获取数据，普通浏览器环境才 fallback 到 `/api/mobile/access-info`。后端返回结构化 `entries`（`id/url/host/role?/isDefault`）与同序派生的兼容字段 `urls`；卡片经 `mobileAccessSelection` 解析入口，优先 `entries`，空时才从 `urls` 回落。默认选中 `isDefault`（后端以 `local_lan_ip` 默认出站 IP 标记），否则首项；`entries.length >= 2` 时渲染 radiogroup 网段芯片切换，`role` 仅 `wifi|wired` 时显示角色标签，否则纯 IP；主链接、复制与二维码始终跟随当前选中 entry，刷新后仍有效则保留选中。警告区固定渲染 `mobileAccess.warning`：中文“同一可达网络中的任何设备均可读取、写入和执行；系统不验证调用者身份”，英文等价声明；禁止“安全/已认证/可信设备”等措辞，不引入 mode UI。
- **Mobile SPA**: `mobile.html` + `src/mobile/` — 独立 `/mobile` Vite 入口，构建产物引用根路径 `/assets/*`，由 Rust fallback 精确服务对应 frontendDist asset；入口加载 tokens/reset/i18n，根组件创建 terminal buffer store、订阅 `/api/workbench/events` NDJSON，并渲染移动 Workbench shell；手机竖屏导航默认收起，用户需要时通过顶部按钮打开（`MobileWorkbenchShell` 窄屏导航走共享 `Drawer side="left"` 原语，禁止手写 backdrop/role=dialog；宽屏固定 rail 在 Drawer 外常驻；**不引入 bottom nav**），默认面板为 projects（有待处理事项时也不自动抢占）。主导航为双模式（不另建路由）：**global**（无项目或在项目列表/Provider）= Projects=`projects`、Inbox=`attention`、Tools=`prompt`、System=`settings/provider`；**project**（已选项目且当前非 projects/provider）= Work=`terminal/browser/files/git/worktrees/automation` + Shortcuts=`attention/prompt/settings`，导航顶部提供返回项目列表；Prompt 为全局入口不绑项目；项目绑定面板无 active project 时回落 projects。分组标题 `section aria-labelledby` 不可聚焦。shell 监听 `visualViewport` 写入 `--mobile-shell-height`（= layout viewport 全屏高度，不压缩）/ `--mobile-keyboard-inset`（键盘高度，驱动 shell `top` 取负值整体上移）/ `--mobile-terminal-min-height` 与 safe-area padding，软键盘弹出时 shell 整体上移到键盘上方（terminal 大小不变，顶部菜单移出屏幕顶部，键盘收起后恢复）；横屏优先终端高度。attention 在独立 Attention 组并显示与桌面相同的 badge 规则，不在顶栏永久增加第二个 attention 按钮。`MobileWorkbench` 挂载后通过 `httpWorkbenchTransport.projects.list()` 读取最近项目；项目详情状态机为 `idle|loading|ready|error`（同项目早退仅 `ready`；`error` 时「重新加载项目」或再点同项目必须 force reload）；选择切换时 abort/作废旧详情请求；连接态 `online|reconnecting|offline` 显示在 shell 状态栏，恢复 online 后刷新当前可见 panel；`kind === "local"` 和 `kind === "remote"` 的项目都可选择，并并行加载 `worktrees.list(project.id)` 与 `sessions.list(project.id)`，成功后进入 terminal；remote shortcut 由本机 `/api/mobile/workbench/*` route 调用 commands 层 remote-aware helper，再代理到 owning device，因此 terminal/browser/files/git/worktrees/prompt/automation 面板都保持同一套移动端导航，不再回落 automation；未知项目类型才用 focusable `aria-disabled` 提示不可用；active worktree 优先主工作区、否则首项，active session 优先 `matching worktree && running`、任意 matching worktree、任意 running、首项，并把 active project/worktree/session 名称传给 shell 状态栏；worktrees 面板用 prefix/suffix 表单创建分支，展示 branch/path/status/sync/canPush，可 merge 或删除非主 worktree 并刷新 active worktree/session，点击工作区卡片成功切换后直接进入 terminal 面板，所有 active worktree 变更都需先经过 Files dirty snapshot guard；删除/merge 这类 destructive 操作只有在会移除 active worktree 时才必须先做 confirm-only guard，后端成功后才清 dirty/discard token 并切换 active；非 active source merge 只移除源列表，不切 active、不清 dirty 草稿；失败或取消时保留草稿和当前 active；files 面板常驻挂载但非 active 时用 `hidden` 隐藏，按 active worktree 浏览目录、打开文件、保存可编辑文本且用 baseHash 乐观锁，图片/CSV/SQLite 等在移动端只读摘要展示，项目/worktree 切换遇到 dirty 文本需由父级先确认丢弃，取消切换后保留当前 opened/draft/dirty/currentDir，切回原 context 不重载根目录，父级已确认丢弃时通过 discard token 跳过 Files 内部重复 confirm，context 切换、清空、根目录重载和保存响应都必须做 request/context/path stale guard；git 面板读取 active worktree 最近提交并提供 Commit/Push/Merge 操作，commit/push/merge 这类长操作返回前必须校验当前 project/worktree 仍匹配，merge 成功后先按删除计划切到回落 worktree 并清空源提交，不能再用可能已删除的源 worktree 拉 commits；prompt 面板调用 `httpWorkbenchTransport.prompt.streamToTerminal(prompt,{workingDirectory: activeWorktree.path,targetLanguage,sessionId})` 把优化结果写入当前终端；terminal 面板使用 `MobileTerminalPanel` 挂载真实 xterm，终端输出区一指上下滑动由 touch handler 消费浏览器页面滚动并映射到 xterm scrollback，通过 `/api/mobile/workbench/sessions/replay` 写入历史、通过外部 buffer store 消费 NDJSON 增量，replay ready 前禁止输入写回，**先订阅并有界暂存 HTTP replay 在途的 exact live delta；replay 成功后按 owner/seq 只拼接高于 `lastSeq` 的 held live，再以结果作为外部 store baseline；随后用 `createTerminalLiveWriter(..., resetStrategy: 'preserveScrollback')` 先订阅 `subscribeLive` 再读 snapshot，按 `generation/appendId` 把 exact delta 直接写入 xterm。移动 xterm 禁止走 `useWorkbenchTerminalBuffer` / 完整字符串 diff 热路径，因为 200k ring 裁头后快照可能滑动甚至保持相同，真实输出会被误判 no-op/重放；同 owner 短 snapshot reset 只切换 cursor 并保留 scrollback，owner change / 显式 clear 仍串行 clear + 权威 replay**，并把输入、resize、focus、新增 pane（固定 down split-pane）、switch-pane、close-pane、close window 全部映射到 `/api/mobile/workbench/sessions/*` tmux-backed routes；移动端不暴露左右/上下分屏选择，只提供新增 pane、切换 pane、关闭 pane；终端面板支持 fixed overlay 全屏模式，全屏时隐藏移动端 shell、标题、window tabs、加载/错误提示等外围内容，只保留 pane 功能行、退出全屏按钮和当前终端输出；全屏 overlay 与 shell 共用 `--mobile-keyboard-inset`（`top = -inset`）在软键盘弹出时整体上移，不得依赖已废弃的 `--mobile-shell-offset-top`；focus/新增/切换/关闭 pane 后都调用 zoomPane 让 tmux window 保持 zoom，只显示当前 active pane；不新增第二套 terminal/pane 模型；项目、详情、worktree 与 session 刷新请求必须用 stale guard 避免旧响应覆盖新选择。
- **API 模块**: `src/api/backend.ts`、`sync.ts`（`trigger_sync` → `SyncRunResult` per-device/domain；Settings 同步 tab 展示 succeeded/partial/unreachable 等，partial 不计成功；另含 `backupApi` 可验证导出/恢复：`create_backup` / `inspect_backup` / `restore_backup` / `list_recovery_jobs` / `list_pre_restore_backups` / `rollback_recovery_job`，以及 `pickBackupExportPath` / `pickBackupArchivePath` 原生路径选择；可恢复领域 token 为 `BACKUP_RESTORE_DOMAINS`，`configReport` 仅导出）、`prompts.ts`、`promptOptimizer.ts`、`scratchpad.ts`、`githubTrending.ts`、`ccHistory.ts`、`claudeMd.ts`、`config.ts`、`devices.ts`、`transfer.ts`（list/send（deviceId+filePath+clientOperationId）/cancel + 幂等 retry/resume（taskId + clientOperationId → TransferTask）+ getOperation（clientOperationId → TransferOperationStatus notFound|pending|succeeded|failed）+ prepareOpen/open/reveal：prepare_transfer_open → LocalTransferOpenTarget；open/reveal 再调 plugin-opener，失败 `transfer_opener_failed`；runtime schema 闭集解码 phase/failure/operation，非法后端值 reject）、`ssh.ts`、`health.ts`、`workbench.ts`、`workbenchDependency.ts` — 各资源方法调 `invoke('命令名', args)`，命令名对应 Rust `#[tauri::command]`，参数 camelCase。`backend.ts` 封装 GUI 管理 sidecar 的 `get_backend_status/start_backend_process/stop_backend_process/exit_gui`，以及 LAN 披露 `get_lan_disclosure_status` / `acknowledge_lan_disclosure_and_start_backend`；关闭弹窗中只有“前后端都关闭”路径可调用 `backendApi.stop()`。`workbench.ts` 按 projects / remote / worktrees / sessions / files / git 分组封装工作台本机项目、远端项目入口、Git worktree、终端 window/pane、当前工作区文件树和 Git 提交树命令；remote API 负责远端 roots/listDir/info/openProject 项目选择入口，远端项目打开后继续复用同一套 worktrees/git/sessions/files API，后端按 remote project/worktree/session id 代理到远端设备；worktrees API 负责 `list/create/commit/push/merge/remove`，git API 负责 `listCommits(projectId, worktreeId?, limit?)` 并按 active worktree 返回提交 DAG、parent hashes 和 local/remote/tag refs，sessions API 名保留历史命名，语义上 `create/list/focus/focused/rename/close` 操作 terminal window，`create(projectId, initialSize, worktreeId)` 会把 window 绑定到 active worktree cwd，`focus(sessionId)` 会同步底层 tmux `select-window`，`focused(projectId, worktreeId)` 读取当前 worktree 的 tmux current window 并返回对应 sessionId，`splitPane(direction)` 在当前 tmux window 内创建 pane，`switchPane(sessionId)` 切到当前 window 的下一个 pane，`zoomPane(sessionId)` 幂等确保当前 active pane 以 tmux zoom 单 pane 视图展示，`closePane(sessionId)` 多 pane 时关闭当前 active pane，最后一个 pane 时关闭所属 window并返回 `closedWindow:true` 让前端移除 tab；files API 均接受可选 `worktreeId`，文件树/新建/重命名/删除只作用于 active worktree 根路径，`open_workbench_file` / `save_workbench_text_file` / `format_workbench_structured_content` / `preview_workbench_sqlite` 分别承载文件打开、文本保存、JSON/TOML/YAML 格式化和 SQLite 只读表预览。`workbenchDependency.ts` 封装 `check_workbench_dependency` / `install_workbench_dependency` / `get_workbench_dependency_install_status` / `cancel_workbench_dependency_install`，供 Workbench 和设置页共享 tmux 依赖状态。普通 Vite 浏览器调试环境缺 Tauri IPC 时，页面只显示友好不可用提示。`promptOptimizer.ts` 封装 `optimize_prompt(prompt, workingDirectory?, targetLanguage?)`、`completeOrchestratorTaskPrompt(prompt,{workingDirectory?})` 和 `streamToTerminal(prompt,{workingDirectory?,targetLanguage,sessionId})`；普通 Prompt 优化页不传目录和语种并返回双语 `optimizedZh/optimizedEn`，项目自动化创建任务弹窗可把简单 Prompt 结构化为 `title/goal/acceptanceCriteria`，Workbench 快捷键小组件传 active worktree 根目录让后端 Claude Code 加载该工作区 CLAUDE.md，同时传设置页选择的 `zh|en` 和 active sessionId，由后端把单语优化结果流式写入终端；远端 sessionId 会由后端代理到远端设备执行优化并写入远端终端；`prompts.ts` 除 CRUD/sync/listTags 外封装 `listVersions` / `restoreVersion`（`list_prompt_versions` / `restore_prompt_version`）；`scratchpad.ts` 封装多页面速记本 9 命令：`listPages` / `getPage` / `createPage` / `updatePageContent` / `renamePage` / `deletePage` / `sync` / `listVersions` / `restoreVersion`（对应 `list_scratchpad_pages` / `get_scratchpad_page` / `create_scratchpad_page` / `update_scratchpad_page_content` / `rename_scratchpad_page` / `delete_scratchpad_page` / `sync_scratchpad` / `list_scratchpad_versions` / `restore_scratchpad_version`）。`githubTrending.ts` 封装 Github热门页 GitHub Trending 5 命令：`list` / `getConfig` / `getDefaultConfig` / `updateConfig` / `testClaudeCli`；`config.ts` 的 `configApi` 封装当前配置 `get`、偏好默认值 `getDefaults`、配置更新 `update`（含基础偏好、截图快捷键、Workbench Prompt 优化快捷键和填入语言），以及云端同步 5 命令：`getCloudSyncConfig` / `getDefaultCloudSyncConfig` / `updateCloudSyncConfig(payload)` / `triggerCloudSync` / `testCloudSync`。 自动更新：`checkUpdate` / `downloadUpdate` / `getDownloadStatus` / `cancelDownload` / `installUpdate`；`UpdateDownloadStatusValue` 为 `idle|checking|downloading|completed|installing|failed|cancelled`。Settings About 在 checking/installing 禁用检查与下载；`completed` 且 `error` 非空表示安装失败可重试（不伪造安装进度条）；轮询覆盖 checking/downloading/installing。- **Attention API**: `src/api/attention.ts` 桌面 `attentionApi.listSnapshot` 调 `list_attention_items`；`src/api/attentionHttp.ts` 移动端 `listAttentionSnapshotHttp` / `attentionHttpApi.listSnapshot` 先 GET `/api/health` 校验 `protocol_version>=1` 且 capabilities 精确含 `attention.v1`，再 GET `/api/mobile/attention`；默认经 `getJson(..., { timeoutMs: ATTENTION_HTTP_TIMEOUT_MS })`（30s AbortController）防半开连接；legacy 无能力抛 `AttentionHttpError(kind=unsupported)`，禁止猜测旧接口。`src/lib/attention.ts` 纯规则：`formatAttentionBadgeCount`（0→null、1..99→数字、100+→99+）、`groupAttentionItems`（空组省略）、`protectAttentionItemOrder`、`getAttentionActionI18nKey`、`buildDesktopAttentionTargetUrl`（仅 `/workbench?...view=automation&taskId|outboxId` 与 `/settings?tab=dependencies`）。类型在 `src/lib/types/attention.ts`（兼容 barrel `@/lib/types` 仍可导入）：`AttentionCategory/Freshness/SourceKind/Target/Item/Counts/Snapshot`。测试：`npm test -- attention`。桌面页 `src/pages/Attention`（`/attention`）：独立 Inbox，仅导航到 `/workbench?...view=automation&taskId|outboxId` 或 `/settings?tab=dependencies`；列表内不执行 Deliver/Retry/Discard/安装；**每行仅一个 `<button>`**（动作文案为内部 visual `<span>`，禁止嵌套 button/role=button）；桌面与 `MobileAttentionPanel` DOM 语义一致；分组“需要你的决定/运行受阻/环境受阻”，空组隐藏；Provider 挂在 AppShell 外层供侧栏 badge 与页面共享。共享键盘 helper：`src/lib/rovingTablist.ts` 的 `getRovingTabIndex`（ArrowLeft/Right wrap、Home/End）；Workbench 终端 session tabs（`WorkbenchSessionTabs`）、inspector tabs、文件 tabs 共用 roving 语义（选中 `tabIndex=0`，其余 `-1`；close 为 sibling 非嵌套；关闭选中 session 后焦点相邻 tab 或新建按钮）；Transfer dropzone Enter/Space 打开原生 picker。Workbench deep link 支持 automation 分阶段：project→automation console→task/outbox focus；目标缺失时 Orchestrator 回报 not-found，刷新 Attention 并回 `/attention`。Settings `?tab=` 以 URL 为真源，挂载后 search 变化同步 tab。
- **Attention invalidation 桥**: `src/hooks/attentionInvalidation.ts` 提供 `requestAttentionInvalidation()`（派发 `cp-attention-invalidate`）与 `subscribeAttentionInvalidation`；`AttentionProvider` 订阅后走统一 `runLoad/refresh`。成功路径必须失效：桌面 Orchestrator Deliver / Request Rework / task Retry / project Refresh / outbox Retry·Discard；移动 Automation outbox Retry·Discard；WorkbenchDependency 用户 recheck 成功、install 成功、安装轮询离开 `installing`。失败 catch 与挂载静默依赖探测不得失效。10 秒可见轮询仍是远端/外部兜底。E2E：`npm run test:e2e -- --project=chromium tests/attention.spec.ts`（确定性 Tauri mock）；单元：`npm test -- attention`。
- **Mobile Inbox**: `src/mobile/components/MobileAttentionPanel.tsx` + `mobileAttentionTarget.ts` — `/mobile` 导航第二项「待处理」；`MobileApp` 挂 `AttentionProvider(loadSnapshot=attentionHttpApi.listSnapshot)` 与 `WorkbenchDependencyProvider`；badge 复用 `formatAttentionBadgeCount`；列表复用 `groupAttentionItems`/`getAttentionActionI18nKey` 紧凑分组渲染（不复制桌面 JSX；每行单 button，动作标签为 span）；点击条目经 `mapMobileAttentionTarget` 跳到现有 Automation（task 详情/Evidence 或 outbox 行）或 Settings 依赖区（`MobileSettingsPanel`+`WorkbenchDependencyCard`）；目标缺失刷新并回 Attention；legacy 无 `attention.v1` 显示 unsupported；有快照时刷新失败保留列表+stale；列表内不执行 Retry/Discard/Deliver。测试：`npm test -- mobilePanelState mobileAttention MobileAutomationPanel mobileWorkbenchState`。
- **局域网防火墙依赖 API**: `src/api/lanFirewallDependency.ts` 封装 `check_lan_firewall_dependency`；Settings 依赖环境页用 `LanFirewallDependencyCard` 展示 P2P TCP 当前端口、mDNS UDP 5353、局域网 IP、平台、端口是否已开放和对应打开方法，并固定展示 `lanFirewall.riskNotice`（无身份校验的读写执行风险 + wildcard 0.0.0.0 监听说明），不自动执行防火墙修改命令。
- **运行诊断（N1 Task6）**: `src/api/runtimeDiagnostics.ts` 封装 `get_runtime_diagnostics` / `open_backend_log_dir`；`RuntimeDiagnosticsCard` 挂在 `SettingsDependenciesPanel`（与 Workbench/Lan 依赖卡并列），展示 owner/generation/Cloud Sync phase（真实 `idle|running|succeeded|failed`）/terminal·bridge 计数/Orchestrator tick·error class，并提供刷新、打开日志目录、复制脱敏诊断摘要；复制 JSON 经 `findForbiddenDiagnosticsKeys` 扫描，禁止 token/content/prompt 等字段；文案在 `settings.runtime.*`。
- **Gap resync / baseline 前端消费（N1 fix M2 + P1 whole-program fix R1–R13）**: `useWorkbenchTerminalBuffers` **listener-first**：先注册 `workbench:terminal-output` 与 `workbench:terminal-resync`，再 `sessions.list()`（**无 projectId，仅本机**）后各 session 走 `beginStartupBaselineReplay` + **`requestSessionReplay`** 统一 baseline（R12 M1，与 authority/overflow 共用 classify / 有界重试 / historySyncFailure；禁止直接 await `sessions.replay` 吞错）。**R13 H1 单飞 cutover**：`beginStartupBaselineReplay` / `beginAuthorityChangeReplay` **保留**既有 `replayInFlight`（不得 list/authority 路径清门闩）；请求以 `requestEpoch` 为所有权 token，同 session 仅一个 in-flight；旧 flight finally 若发现 epoch 已抬高则串行启动唯一替代请求，禁止并发 snapshot 互相 `store.reset` 抹掉更高 seq live。**R13 M1 启动 list 恢复**：`sessions.list` 有界退避最多 `STARTUP_SESSION_LIST_MAX_ATTEMPTS`（5）次；仅成功枚举并 schedule 各 session 后才 `baselineSettled=true`；永久失败写入可观察 `startupBaselineFailure{kind:startup_list_failed}`（Context：`getStartupBaselineFailure` / `subscribeStartupBaselineFailure` / `getStartupBaselineFailureRevision` / `retryStartupBaseline`；`useStartupBaselineFailure` + Workbench `StatusMessage` `terminal-startup-baseline-failure` 可手动重试）；失败期间 held 窗口仍 settle 以免永久膨胀，但历史依赖后续 live/resync 或手动重试。**held 仅在异步 baseline/replay 窗口收集**（`!baselineSettled || needsReplay || replayInFlight`；`shouldCollectHeldLiveTerminalEvent`）；steady-state live **不**入 held，避免每 257 个 chunk 强制 overflow replay。**per-session cutover 状态**（`SessionCutoverState{committedBaselineLastSeq,cutoverEpoch,needsReplay,replayInFlight,overflowHighWaterSeq,authorityId}`，pure helpers 在 `workbenchTerminalBuffer.ts`）：resync/baseline/live/`sessions.replay` 均携带可选 `ownerInstanceId`；**remote bridged session 的 replay/resync/live 使用 composite authority（local bus + remote stream owner，R8 H1）**；远端 backend 重启导致 remote owner 变化时 authority cutover 重置 lastSeq，避免静默冻结。**authority 首次绑定 / 已绑定切换（R9 M1 + R10 M1）**：`beginAuthorityChangeReplay` 在 **state.authority 为空的首次绑定** 与 **已绑定切换** 时一律抬 epoch、`needsReplay=true`、清旧 held、立即 `sessions.replay`（保留 inFlight）；**禁止** light rebind 且 needsReplay=false（启动 list 不含远端 session，首条 remote live 若跳过 replay 则 bridge 前 ring/TUI 历史永不补回；远端 `/api/workbench/events` 是纯 broadcast，断线窗口也不会进本机 Gap）。仅接受匹配新 authority 的 replay；成功 cutover 后 settle held（`seq > lastSeq`）。resync 路径在 authority 变化时抬 epoch 作废 in-flight，但以 resync 自身 buffer 为 baseline（`requestEpoch` 清 needsReplay），不额外再拉 replay。同 generation 下 live-before-replay / replay-before-live 补历史且不双写；local 会话仍用纯 local owner；`shouldAcceptTerminalCutover`——**已绑定 authority 时：缺 owner 或其它 owner 的 cutover 一律 reject**（防 delayed owner-A replay clobber B，R6 H2）；首次绑定（authority 为空）可 accept 新 owner。否则 `lastSeq < committedBaselineLastSeq` 或 `requestEpoch < cutoverEpoch` 则 **reject**。accept 后 `applyTerminalBaselineCutover`（authority 变更时丢弃旧 held；否则 re-append `seq > lastSeq`）并 `commitTerminalCutover(..., authorityId)` 绑定 authority。**held 溢出禁止静默 drop-oldest**：highWater → `beginHeldOverflowReplay` → `sessions.replay`。**replay 失败恢复（R10 M2 + R11 M1）**：同 epoch 立即最多 `TERMINAL_REPLAY_IMMEDIATE_ATTEMPTS`（3）次；耗尽后仅对 **可恢复** 错误（timeout/unavailable/network；`classifyTerminalReplayError` → `recoverable`）走 `terminalReplayRecoveryDelayMs` capped backoff（封顶 `TERMINAL_REPLAY_RECOVERY_BACKOFF_CAP_MS`）持续可取消重试；后续 live 在 `shouldTriggerTerminalReplayRecovery` cooldown 到期后可 re-trigger；成功 cutover / removeBuffer / unmount 清理 recovery 状态。**永久错误不得无限重试**：`not_found`（会话不存在）→ `stopTerminalCutoverReplay` + 清理 recovery，暴露 `getHistorySyncFailure` kind=`not_found`；稳定 code `validation`/`validation_error` 或 `ContractDecodeError` → 停止自动重试，暴露 kind=`history_sync_failed`（R12 M2 禁止 message includes）；受控 `console.warn` 仅稳定 token，禁止 body/path/token。Context 暴露可订阅 `getHistorySyncFailure` / `subscribeHistorySyncFailures` / `getHistorySyncFailuresRevision` / `retryHistorySync`；`useTerminalHistorySyncFailure` + Workbench `StatusMessage`（`terminal-history-sync-failure`）展示 notFound/failed 并可 retry（R12 M3）。`classifyTerminalReplayError` **只**认稳定 `code` / `ContractDecodeError.name`，禁止 message 子串。removeBuffer 清理 held、cutover、recovery 与 history-sync failure。store `append/reset` 亦按 authority 分代比较 lastSeq。多机远端重启 cutover 的 L3 仍 **NOT VERIFIED**。`terminalInputPump`：旧 generation 的 in-flight write reject **不得**清新 generation pending / block / onWriteError。`useOrchestratorRuntimeSnapshot` 监听 `backend:runtime-gap` 强制刷新当前 project snapshot。
- **运营通知协调器（N6 Task6 / A2）**: `useOperationalNotifications` + `OperationalNotificationCoordinator` 挂在 `AttentionProvider`/`ScratchpadAutosaveProvider` 内（`App.tsx`）；先注册 `operational:notification`/`backend:runtime-gap` listener 并按 `(ownerId,sequence)` 缓冲，再 `operationalNotificationsApi.getSnapshot`（`get_operational_notification_snapshot`）建立 no-notify baseline，丢弃 `<=asOfCursor` 后 drain 更大 cursor；**await maybeNotify 期间新入 buffer 的事件必须再扫直至空，并在切 live 后 residual 冲刷一次**（防 mid-handshake 搁浅）；drain 后 cursor 抬到 max(asOf, drained sequences)。Gap/owner change 暂停并 re-handshake 不卸载 buffer listener。dedupe key=`kind:opaqueSourceId:stateVersion`（Agent 同 phase 低噪音由后端 phase-edge emit 保证）；偏好 `notifyHumanReview/notifyBlocked/notifyRemoteOutboxFailed/notifyTaskDone`（默认 on/on/on/off）来自 `orchestratorConfig`，Agent `notifyAgentNeedsInput/notifyAgentFailed/notifyExperimentDecision` 默认 on/on/on；权限经 `checkNotificationGranted`（不循环 request）；前台 authority（`visibilityState=visible` 且 path 为 `/attention` 或 `/workbench`）抑制 OS 通知但仍 `requestAttentionInvalidation`；`sendOperationalNotification({title,body})` 仅固定 i18n 文案（`orchestrator:notifications.*`，含 agentNeedsInput/agentFailed/experimentDecision），禁止 actionType/extra/onAction 与任务标题/goal/路径。测试：`npm test -- useOperationalNotifications.test.tsx operationalNotifications.test.ts`。
- **Orchestrator 前端**:
  - `src/api/orchestrator.ts` 的 Workbench/Orchestrator active project 流程只走 remote-aware task view 命令：`list/create/start/retry/requestRework/deliverReviewed/cancel/refresh_orchestrator_*、`retry_orchestrator_remote_outbox`/`discard_orchestrator_remote_outbox`、`list_orchestrator_task_evidence_for_project`，以及本机项目看板命令 `move_orchestrator_task_workflow_state` / `get_orchestrator_runtime_snapshot`；create 请求统一传 `createAction: 'backlog' | 'todo' | 'start'`，缺省 Backlog，并原样透传 tracker 预留字段。旧裸 task 方法仅兼容展开 local/remote task，不得调用旧本机-only 命令，也不得调用 legacy `get_orchestrator_config_for_project`。
  - `src/lib/types/orchestrator.ts`（兼容 barrel `@/lib/types`）的 Orchestrator task DTO 保留 legacy `status`，并额外对齐后端 split-state/runtime 字段 `workflowState`、`runState`、`attemptPhase` 与 runtime metadata；tracker 预留字段为 `source/externalId/externalIdentifier/externalUrl/externalState/externalLabels`，其中 `externalLabels` 类型是 `string[] | null`，前端只透传/展示数据，不用这些字段驱动看板分组、selection、action 或 scheduling；runtime snapshot DTO 必须包含 `projectKind/remoteStatus/generatedAt/latestTickAt/lastDispatchAt/lastDispatchedCount/runningTasks/retryingTasks/recentEvents/latestError`，`remoteStatus` 为本机 `local` 或远端四态 `live|unsupported|offline|unavailable`（类型上拆为 `OrchestratorRemoteRuntimeStatus`）；另有显示态 `OrchestratorRuntimeDisplayState{snapshot,remoteStatus,cachedAt,loading,error}` 供桌面 hook/移动 store 复用，running/retrying summary 与 recent event 字段保持前端可直接展示。
  - `src/lib/orchestrator.ts` 固定 legacy 状态顺序、分组任务，集中提供 `canStart/canRequestRework/canDeliver/canCancel` 等按当前 active project 判断的显式 action helper，把 legacy status、workflow state/evidence kind 映射到现有 `PillTone`，并集中生成 attempt/progress 文案；`src/pages/Orchestrator/orchestratorBoard.ts` 提供纯 workflow 看板泳道顺序、按 `task.workflowState` 分组、active run state 判断和本机相邻泳道拖拽规则；`src/lib/orchestratorRemote.ts` 负责把 `OrchestratorTaskView[]` 拆成 local/remote 真实任务和 pending remote outbox，pendingRemote 只展示设备、状态和错误，不参与状态分组、selection、action 或 evidence。
  - `src/pages/Orchestrator` 只提供任务看板、选中任务后的右侧详情/Evidence 抽屉、创建任务弹窗和编排状态，不默认展示详情/Evidence 两栏，也不展示项目级策略/配置；任务列表加载完成后必须始终渲染固定 workflow 泳道，即使当前项目没有任务也显示空泳道，不用 empty prompt 替代看板；创建任务入口必须是独立弹窗，PC/Workbench 嵌入场景创建弹窗走共享 `Dialog` 原语（body portal / focus trap / Escape·backdrop；busy 时 `closeOnEscape/closeOnBackdrop=false` 且 `handleCloseCreateDialog` early-return），任务详情/Evidence 走右侧 `Drawer`，页面不得手写 `role=dialog`/`aria-modal`/`createPortal`/window Escape 监听；弹窗支持手动填写标题/目标/验收标准，也可通过 `promptOptimizerApi.completeOrchestratorTaskPrompt` 把简单 Prompt 填充为三字段；桌面提交按钮固定为“创建到 Backlog / 创建到 Todo / 创建并启动”，由用户按下的按钮决定 `createAction`，不得用单一提交按钮隐式默认 Backlog；列表和 evidence 必须绑定 `projectId`，evidence/action 还必须绑定 `taskId`，项目或任务切换后旧响应不得覆盖当前 UI；列表刷新失败时保留当前项目已有 task views 和 selected task，只展示错误；任务详情展示 current attempt、执行现场 link、latest verifier result、repair prompt evidence、development attempt history 和 remote execution device，并按状态展示 start、retry、request rework、deliver、cancel、open execution site；创建 remote 离线返回 pendingRemote 时只插入 pending 区，不选中详情；remote 真实 task 使用后端已映射的 `task.projectId/worktreeId/sessionId` 打开执行现场；`complete_orchestrator_agent_run` 仍为 local-only，remote running task 不显示该按钮。OrchestratorPanel owns task dashboard state，Workbench owns project-level console mounting、项目上下文和 execution-context takeover。
- **Orchestrator 配置 API**: `src/api/orchestratorConfig.ts` 只封装设备级自动化配置 `get_orchestrator_config` / `get_default_orchestrator_config` / `update_orchestrator_config`；不要混入任务 API `orchestrator.ts` 或基础配置 `config.ts`。Settings 自动化表单 helper 位于 `src/pages/Settings/automationSettingsState.ts`，保持纯函数，无 React/副作用，验证命令 textarea 文本只做行尾归一，trim/filter 交给后端。
- **Workbench deep link**: `src/pages/Workbench/workbenchDeepLink.ts` 统一构造和解析 `/workbench?projectId=...&worktreeId=...&sessionId=...` 以及 Attention 的 `view=automation&taskId|outboxId` 与 WORKFLOW 的 `view=files&path=WORKFLOW.md`，空参数归一为 `null`；`path` 经 `isSafeWorkbenchRelativePath` 拒绝绝对路径与 `..` 穿越；remote task link 必须保留 `remote:<device>:...` 项目/worktree/session 前缀并交给 `URLSearchParams` 编码；Workbench 按 project →（automation 打开控制台 / files 在 worktree 就绪后 `openFileByPath` / 否则 worktree → session）分阶段应用，`location.search` 变化才重置应用标记；automation 链接不 focus 终端；files 链接不 focus session。file controller 暴露 `openFileByPath`；automation controller 注入该 bridge，Workbench 只接线、不新增第八个 controller。
- **Workbench HTML/Markdown 预览资源**: `workbench.ts` files API 包含 `previewHtmlAsset(projectId, documentPath, assetPath, worktreeId?)`，对应后端 `preview_workbench_html_asset`；前端只把当前 worktree 根内相对资源交给后端，外链、data/blob、绝对路径和加载失败资源在 HTML srcDoc 或 Markdown 图片 DOM 中置空。Markdown 预览通过 Tiptap Image extension 渲染图片节点，DOM 初始 `src` 为空，资源加载成功后才替换为 data URL，避免把 data URL 写回源 Markdown。
- **Workbench 浏览器预览基础**: `workbench.ts` / `workbenchHttp.ts` 的 `browser.discover/createPreview` 分别调用 Tauri `discover_workbench_browser_targets` / `create_workbench_browser_preview` 与 `/api/mobile/workbench/browser/*` routes；`WorkbenchBrowserWorkspace` 是桌面和移动端共享 domain 组件，只消费 `WorkbenchTransport`，`getWorkbenchBrowserFrameSrc(preview, surface)` 负责 desktop 绝对 proxy URL 与 mobile 同源 proxy path 的选择；候选来源显示必须用 `target.source` 经 `getWorkbenchBrowserTargetSourceLabelKey` 映射到 `workbench:browserPreview.sources.*`，不得把后端 `target.label` 当用户可见文案；移动端 `MobileBrowserPanel` 固定传 `surface="mobile"` 和 `httpWorkbenchTransport`，iframe 必须走 `mobileProxyPath` 同源代理；预览 iframe 必须设置 sandbox，允许 `allow-scripts` 等基础预览能力但不得包含 `allow-same-origin`，避免项目预览 JS 同源访问 cc-partner `/api/mobile` 或 `/api/workbench`；桌面 Workbench 的 `workspaceView` 为 `terminal/browser/files`，browser 与 terminal/files 是同级绝对叠层，切换时不能卸载 terminal sessions；组件内 discover/createPreview 这类异步结果必须同时校验 request sequence、projectId 与 worktreeId，旧请求不得写入 preview/manualUrl/error/discovery，busy 也只能由当前最新请求清理。
- **Workbench 浏览器自动验证（A5）**: `WorkbenchBrowserVerificationPanel` 挂在 Browser Workspace 内，默认一键「验证当前预览」；`transport.browser.startVerification(previewId, requestId)` 只传 live previewId（不传 targetUrl/selector/脚本）；桌面 invoke `start/get/cancel/get_workbench_browser_verification_artifact`，mobile 走 `/api/workbench/browser-verification/*`；UI 展示截图 data URL、console 错误数与 assertion 失败数，不提供任意 JS/selector 输入；helpers 在 `workbenchBrowserVerification.ts`；i18n `workbench:browserVerification.*`。
- **Workbench 浏览器预览验证**: 由 `npm test` 统一收集 `src/pages/Workbench/workbenchBrowserPreview.test.ts`、`src/mobile/mobileBrowserPanel.test.ts`；类型检查走 `npm run build` 的 `tsc -b`。
- **Settings 资源隔离**: `src/pages/Settings/settingsResources.ts` 按资源组（core/cloudSync/github/health/automation/version 等）独立加载；单组失败只标该组 error/retry，不得整页失败或清空已成功组；defaults 不可用时「恢复默认」禁用并提示。`useSettingsController` 消费各组 load/error/retry 并负责 apply/retry 与 dirty 保护；`Settings.tsx` 壳层只读 controller 结果做 loading/core-error 与 tab 组合。**常规 tab 保存失败**写入 `saveError`（`SettingsGeneralPanel` footer 展示）并保留脏表单与 `isDirty`；禁止把 save reject 写进 `loadError`（会整页 early-return 卸掉草稿）。**Safe-save 合同**（`src/lib/asyncState/saveAttempt.ts`）：General / Cloud Sync / GitHub·AI / Prompt Optimizer / Health / Automation 六个保存路径各自持有 `editVersion` + `requestSeq`；用户每次编辑递增 version，submit 捕获 `SaveAttempt`；success 总是更新 baseline，仅 version 未变且 draft 仍等于 submittedSnapshot 时才 hydrate；旧 seq 的 success/error 不改当前态；失败保留 draft 且错误 scoped 到该资源。ClaudeMd 页 save/push 同合同（`ClaudeMd.tsx` + `ClaudeMd.test.tsx`）。
- **Settings controller/panel 边界**: `Settings.tsx` 仅做 URL tab、tablist 与 panel 组合；≤680px 深链选中 tab 由 tablist 自身 `scrollTo` 显示（不滚动页面主容器；用 getBoundingClientRect 相对 tablist 换算 scrollLeft，禁止依赖 offsetLeft/offsetParent，避免把选中 tab 左侧裁切；behavior=auto + 贴边二次校正），roving tabindex + Arrow/Home/End 保持；全部表单 state、资源加载/保存/reset、权限与更新动作在 `useSettingsController.ts`；`SettingsGeneralPanel` / `SettingsSyncPanel` / `SettingsDependenciesPanel` / `SettingsAiPanel` / `SettingsAboutPanel` 与既有 `HealthPanel` / `AutomationSettingsPanel` 均为 pure props 视图，禁止 transport 调用。`SettingsSyncPanel` 可从 `@/api/sync` 导入类型与 pure helpers（`isDeviceSucceeded` / `BACKUP_RESTORE_DOMAINS` 等），但 **禁止** 调用 `syncApi` / `backupApi` / `invoke`——导出/恢复/LAN 同步全部由 controller 编排（含 restore/rollback Dialog busy 门闩）。LAN 同步卡片按 device/domain 展示 `Succeeded|Partial|Unreachable|ProtocolError|ResourceLimit` 与 pulled/pushed/unchanged，**仅** `isDeviceSucceeded` 设备计入成功数。备份区：先 inspect 预览 → Dialog 确认 domain/mode → restore；configReport 只读不写回；jobs 列表可 rollback。静态 ownership：`settingsOwnership.test.ts`；AI panel 交互：`SettingsAiPanel.test.tsx`。**Settings controller 组成**：`useSettingsController` 为 thin composer（URL tab + 三域 hook）；`controllers/useSettingsResources` 负责 load/retry/loadError/version；`controllers/useSettingsFormSaves` 负责表单草稿与 safe-save；`controllers/useSettingsUpdatePermissions` 负责权限与自动更新。- **Settings AI tab**: 配置本机 Claude Code CLI。CLI 路径与模型同时影响 GitHub 项目解说和 PromptOptimizer；`aiEnabled` 与 `cacheTtlHours` 只作用于 GitHub 项目解说，PromptOptimizer 始终是单次调用且不缓存。AI tab 还管理 Workbench Prompt 优化浮层的页面内快捷键与自动填入语言，默认快捷键为轻按 Control 单键（只在按下并松开 Control 且没有组合其他按键时触发），默认填入中文优化版；表单/handlers 归属 `useSettingsController`，视图为 pure `SettingsAiPanel`（壳层透传 controller props）。
- **Settings 自动化 tab**: 由 `AutomationSettingsPanel.tsx` 纯渲染 Orchestrator 全局自动化配置（启用、并发、验证命令、auto commit/push/merge/push main、运营通知四开关），状态、加载、保存和恢复默认在 `useSettingsController`；tab 深链支持 `/settings?tab=automation`，用户可见文案走 `settings.automation`；通知偏好字段 `notifyHumanReview/notifyBlocked/notifyRemoteOutboxFailed/notifyTaskDone` 经 `orchestratorConfigApi` 持久化，panel 保持 API-free。
- **页面**: `src/pages/` — Home（GitHub Trending Weekly Top 25 项目列表，桌面双列瀑布流，奇偶排名分列以形成错落卡片流并避免按行 grid 留空；窄屏单列按原始顺序展示；卡片排名与标题同行且卡片自身高度不强制等高，卡片内部按头部/简介/Claude 解说/指标分区保留明确间距，展示原始简介 + 本地 Claude Code CLI 生成的中英文解说，仓库外链经 `@tauri-apps/plugin-opener` 打开系统浏览器）/ Prompts / CcHistory / Scratchpad / PromptOptimizer / AgentHub（`/agent-hub` Multi-CLI 指令与 portable 资产工作区：CLI probe、scope/kind 过滤、canonical 名与 invocation alias 分离、aggregateStatus 汇总 `full|partial|sourceOnly|activationRequired|externalCollision|detached|blocked`、三端 target cells（requested/supported/sourceOnly/verified + presence/enabled/mat）、enable/disable 单 target、portable 行内多动作组（enable/disable + uninstall capability 驱动）、detached restore/remove、delete everywhere 确认、externalCollision 预览 Dialog、项目 preview Dialog、冲突 Drawer、InstructionBlocksDrawer；LAN push 文案标明 Gate C，本版本不提供跨设备 push；旧 ClaudeMd 源码保留 N/N+1；ClaudeCodeAssets 前端已删除，权威资产 UI 为 AgentHub portableAssets）/ ClaudeMd / Workbench（`/workbench` 项目工作台：入口来自全局左侧栏 Work 组内的 `WorkbenchProjectRail` 项目文件夹列表，不再占用一个独立主导航项，项目卡片右下角显示该项目 terminal window 数与 pane 总数；主区域按“工作台标题 + terminal sessions badge / worktree 管理层 / 依赖提示槽 / 当前 worktree 的 window tabs / 随应用主题切换的终端面板”组织，Project → Worktree → Window → Pane 是页面层级；worktree 管理层支持切换主/功能 worktree、新建 worktree 和移除非主 worktree；Git 历史 tab 顶部承载当前 active worktree 的 clean/dirty/conflict 状态以及 AI commit、push、merge 到主工作区操作；Commit 按钮不弹手写 message，不依赖可能过期的 clean 状态禁用，而是调用 `commit_workbench_worktree(worktreeId,message:null)`，由后端在 active worktree cwd 下用 Claude Code 项目上下文模式根据 staged diff 生成 commit message 并提交；无可提交改动时后端返回最新 worktree DTO，前端刷新状态而不弹错误；tmux 依赖非 ready 时展示 `WorkbenchDependencyCard`，用户可安装、取消、重新检测；进入项目时 `list_workbench_sessions(projectId)` 由后端恢复持久化 terminal window，macOS/Linux 有原生 tmux、Windows 默认 WSL 发行版有 tmux 时可继续原 shell/Claude 上下文；前端终端挂载层保留当前项目下所有 terminal window 的 xterm 实例，只让 active worktree 的 active window 可见/可交互，inactive 常驻 xterm 产生的 `onData` 必须丢弃，pane 布局由 tmux 在 xterm 内渲染；工具栏的 Prompt 优化按钮打开非模态单输入框浮层，每次从关闭态重新打开都必须清空原始输入；页面内快捷键默认轻按 Control：首次触发打开浮层并聚焦原始 Prompt 输入框；浮层打开后再次触发快捷键时，输入为空直接关闭浮层，输入非空才调用 `promptOptimizerApi.streamToTerminal(prompt,{workingDirectory: activeWorktree.path,targetLanguage: promptOptimizerFillLanguage,sessionId: activeSession.id})`；输入框内非空时按 Enter 与再次触发快捷键等价，Shift+Enter 保留换行，输入法 composing 状态下的 Enter 不提交；优化时使 Claude Code 获得当前 worktree 的 CLAUDE.md 上下文且只生成设置页语种的 Prompt，并由后端把优化文本边生成边写入 running active session，不追加 Enter，完成后自动关闭浮层；浮层内部不得显示“优化/填入终端/中文优化版/English optimized/关闭”等按钮或结果区；终端工具栏按钮与文件预览工具栏保持一致，均显示图标和文字，并提供“适应尺寸”按钮把当前 running session resize 回桌面端可见 viewport；文件预览按钮只在已有打开文件 tab 时启用，点击后恢复当前或首个文件 tab，与文件预览层的返回终端按钮形成对称切换；工具栏的左右/上下分屏按钮调用 `split_workbench_pane`，关闭 pane 调用 `close_workbench_pane`，不再使用前端 double/quad 伪分屏，最后一个 pane 被关闭时同步移除所属 window tab；切换 worktree 或 app tab 时必须保留所有 terminal window 的 xterm 实例，只隐藏非活动 window，不能卸载后 replay 原始终端流，同时 activeSessionId 变化必须调用 `focus_workbench_session` 让 tmux `select-window` 切到底层绑定 window；用户从 tmux status bar/快捷键切换 window 时，页面按轻量轮询调用 `get_focused_workbench_session`，但只同步到当前 worktree 内的 window；宽屏右侧检查器显示当前 window 状态，并在 tab 中切换 active worktree 文件夹与 Git 提交树；Git 历史 tab 顶部放置状态/Commit/Push/Merge 工具条，Push 按钮只在 status.canPush 为 true 时启用（本地未发布且没有 origin/upstream 的项目禁用，只有 `*-upstream` 这类源码上游 remote 也禁用），下方用多 lane 图形展示分支/合并线，提交行必须紧密相邻且图线不能被 card 间距打断，并用 local/remote/tag badge 标识本地与云端 ref，窄屏检查器排到首屏终端之后；创建终端前用离屏 xterm + FitAddon 测量当前 pane 的 cols/rows 并传给后端作为 PTY 初始尺寸，避免交互式程序首屏按默认列宽绘制后错位；xterm 必须 open 到无 padding 的 `.terminalViewport`，视觉 inset 留在 `.terminalHost`/viewport 定位中，避免 FitAddon 把 padding 算进列宽导致内容超出可视区域；`mainWorkspace`/`terminalLayer`/`terminalArea`/`terminalPanel`/`terminalPaneFrame` 高度链必须把 absolute 终端层撑满 centerPane 的 1fr 行（height:100% + terminalArea grid 1fr），禁止仅靠 `.terminalHost{min-height:320px}` 决定可绘制高度——否则冷启动 FitAddon 量出 ~90x24，tmux status bar 会悬在终端中间，「适应尺寸」也只把后端 resize 到同样小尺寸；xterm options 禁止启用 `convertEol`，PTY/tmux 输出必须按原始控制序列渲染，避免分屏整屏重绘错位；桌面 `WorkbenchTerminalPane` 必须挂 `attachCustomWheelEventHandler`：alternate screen 一律拦截滚轮，固定打在 transcript 左上后再发 SGR 64/65（禁止按指针坐标：底部输入栏不滚，status 行会让 tmux 切 window 串历史）；normal buffer 仍交给 xterm 本地 scrollback；xterm `lineHeight` 固定为 1，且 `.terminalViewport .xterm` 禁止 `height:100%`（否则 canvas 下方留白，tmux status 视觉悬空）；「适应尺寸」即使 cols/rows 未变也必须回传后端，由后端 bump 尺寸强制 SIGWINCH/redraw；buffer 截断导致必须 replay 历史输出时，replay 期间必须屏蔽 xterm `onData` 写回，且 gate 释放需延后一轮 macrotask，避免历史 tmux 设备查询响应变成 shell 可见字符；活跃 xterm 已经写入旧 buffer 后，如果常驻 buffer 达到上限并等长滑动截断，前端必须按旧 buffer 后缀与新 buffer 前缀的重叠追加新尾部，不能只比较长度，否则终端会停在旧状态栏时间；文件树支持刷新、新建文件/文件夹、重命名、删除确认、复制相对路径和基础元信息，所有文件操作绑定 active worktree 根目录；点击文件节点打开中心 B1 文件 tab 工作区，预览 tab 标签直接显示文件相对路径，格式化/保存/返回终端按钮与 tab 同行显示，不再渲染第二行文件头部或类型标签；文件工作区与终端区叠层切换，隐藏终端时不能卸载 xterm DOM 或接收输入；图片只读预览，CSV 只读表格，SQLite 只读表/行预览，代码/文本使用 CodeMirror 高亮编辑，Markdown 支持 source/preview/split 且 preview 可 WYSIWYG 编辑，HTML 支持 source/preview/split 且 preview 使用 sandbox iframe 渲染，JSON/TOML/YAML 支持格式化并在保存前做语义校验；文件 tab 保存用 baseHash 乐观锁，dirty tab 重开不能刷新保存基线，关闭 dirty tab 或删除含 dirty tab 的路径必须先确认，open/save/format/sqlite 预览/目录加载请求需做 stale/order guard，重命名/删除路径后同步或关闭已打开 tab 并清理受影响目录子树缓存；项目/worktree 请求回写前需比对当前 projectId/worktreeId，避免旧请求污染新项目 UI）/ Health / Settings（`dependencies` 页签展示 Workbench tmux 依赖状态与安装/重检入口）/ Transfer / Devices / SSH
- **Workbench 终端滚轮分流（覆盖页面长段中的旧 alternate-only 表述）**: `mouseTrackingMode !== 'none'` 时必须优先把滚轮发给 TUI（即使当前是 normal buffer），先让 xterm 回到底部，再向 transcript 左上发送 SGR 64/65；只有未启用 mouse tracking 的 normal buffer 使用 xterm 本地 scrollback，避免 Claude main-screen 全屏重绘在历史区重复最后一页。
- **Orchestrator 面板**: `src/pages/Orchestrator` 导出 `OrchestratorPanel`，在 Workbench 项目自动化控制台中以 `embedded` 模式渲染，隐藏页面级 header。**所有权拆分**：`controllers/useOrchestratorController.ts` 持有任务列表/selection/evidence/create/outbox/deep-link 全部 state、effects 与 `orchestratorApi`/`promptOptimizerApi` 调用，并导出 typed view props；`Orchestrator.tsx` 只组合 snapshot 状态条 + `views/OrchestratorBoard` + `OrchestratorOutbox` + `OrchestratorTaskDrawer` + `OrchestratorCreateDialog` + `views/WorkflowWizardDialog`；`orchestratorViewHelpers.ts` 放纯 label/tone/format helper。**views 禁止** `@/api/*` / transport；controller **禁止** Drawer/Dialog/board JSX。controller 还持有 WORKFLOW 向导 state：`get_workflow_document`/`validate_workflow_document`/`save_workflow_document`（CAS expectedHash；冲突 `workflow_document_changed` 保留草稿 + reload）；missing 用模板预览/创建，valid 展示摘要并可 `view=files&path=WORKFLOW.md` 打开文件工作区，invalid 聚焦诊断行；保存成功刷新 runtime snapshot，**不**自动 dispatch，也**不**改变 delivery。默认只展示 workflow 泳道看板、runtime snapshot、pending remote outbox 和创建任务入口，不自动选中第一条任务。真实任务按 `ORCHESTRATOR_BOARD_LANES`/`workflowState` 分组，pending remote outbox 单独展示；仅 `status=failed` 显示 Retry/Discard（Discard 需 confirm），成功后刷新 task-view 列表，Inbox 列表不执行这些动作；点击具体任务后才通过共享 `Drawer`（side=right）展示 Summary/Evidence 两 tab（roving `tablist/tab/tabpanel`，Arrow/Home/End）与 footer 显式 action，关闭抽屉后回到纯看板；仅本机非活跃任务允许相邻泳道拖拽移动。A0 后无 Changes tab / 人工 digest 门禁；Human Review/Rework 下 Deliver 仅受 Settings full-auto delivery 约束；Rework 用 Dialog 必填 1–2000 字。本机与远端项目状态条统一走 `useOrchestratorRuntimeSnapshot`：按精确 project shortcut ID 拉取 `get_orchestrator_runtime_snapshot`，display state 记录 owning projectId，A→B 首帧立即隔离；模块级 Map **仅缓存 remote live** 成功快照与客户端收到时间（local 成功不写 live cache；进程内 display-only，与 mobile Map 彼此独立，不写 localStorage/SQLite/磁盘，不导出 Map）；request sequence + mounted ref 防 project 切换/卸载 stale 写入，并校验响应 `snapshot.projectId === target`；远端 live 直接展示 owning device 数据；live→offline 仅在后端 DTO `remoteStatus=offline` 或 adapter 抛出 `OrchestratorRuntimeTransportError(kind=network)` 且存在本项目 live 缓存时保留并标 `cachedAt`；禁止 Error.message 关键词猜 offline；非 network transport 失败不得仅因缓存存在标 offline；cold offline、unsupported、unavailable 用各自空态文案且不得跨项目复用缓存；缓存只用于状态条显示，不能进入 task action 或 scheduler 决策；状态条提供 refresh 与 `/settings?tab=automation` 链接，项目刷新仍调 remote-aware `refreshProject`。旧 `/orchestrator` deep link 重定向 `/workbench`。自动化配置只在 Settings 自动化 tab 中用户可见，面板不得展示项目级策略/配置。面板复用 `Button/Card/Input/Pill/Dialog/Drawer`，hooks 必须放在所有渲染分支前；创建任务表单不得固定占用页面 grid，必须由队列区域按钮打开共享 `Dialog`（busy 时 closeOnEscape/closeOnBackdrop=false），AI 完善只填充表单不直接创建任务；用户可见文案走 `orchestrator` namespace，CSS 颜色/间距/圆角/阴影只读 tokens。静态合同：`npm test -- src/pages/Orchestrator`（含 `orchestratorOwnership`）。

- **Workbench HTML 预览**: HTML tab 的 source/preview/split 保持 sandbox iframe；进入 preview/split 前会重写核心 `src`/`href`/`poster`/`srcset` 资源为 data URL，并支持外部/内联 CSS 的相对 `url()` 二次重写。资源路径以当前 HTML 或 CSS 文件为基准解析，后端只读且限制在 active worktree 根内。Markdown tab 的 WYSIWYG/split 图片同样使用该资源通道，但只改 DOM 展示，不改源 Markdown 字符串。
- **Workbench 导航约定**: 终端和文件预览顶部栏复用 `WorkbenchWorkspaceNav`；浏览器预览入口位于终端工具栏，切换到 `browser` workspace view，返回终端时统一回到 `terminal`；项目自动化入口位于 Workbench 标题区，文案为“项目自动化 / Project Automation”，不放在终端工具栏，也不是 terminal/browser/files 的 workspace view。“项目自动化”按钮是控制台开关：打开后再次点击直接切回终端，控制台内部不再提供“返回终端”按钮。自动化控制台顶部必须显示“范围：项目”和当前项目名；控制台打开时 worktree 管理条必须完全隐藏，终端可隐藏但 xterm DOM 不能卸载或接收输入，自动化控制台自身只在打开态挂载可见面板，并作为中心工作区正常文档流内容撑开高度，避免隐藏或绝对定位空层留下黑屏；任务执行后才通过 deep link 绑定 worktree/session 执行现场。桌面端终端全屏按钮位于 pane actions 导航栏，全屏时 `terminalLayer` fixed 覆盖 Workbench 外围内容，保留 terminal window tabs、pane 操作、退出全屏按钮和当前终端输出，确保全屏中仍可切换 window；终端文件预览按钮位于工具按钮组最右侧；文件预览 tab 标签显示相对路径，文件操作按钮与 tab 同行显示。终端和文件预览操作按钮必须按 `WorkbenchWorkspaceNav` 自身宽度响应式收缩：空间受限时隐藏按钮文字、保留图标、`title` 与 `aria-label`，空间恢复后显示完整文字。
- **Workbench 代码编辑器主题 / 语言包**: `WorkbenchCodeEditor` 必须通过 `theme` prop 显式传入 token 化 CodeMirror theme，编辑器背景、左侧行号 gutter、当前行和搜索/面板 chrome 跟随应用主题 token；语法高亮保留 One Dark Pro 色系，避免 `@uiw/react-codemirror` 默认 light theme 把暗色工作台刷成白底。语言扩展经 `loadWorkbenchLanguage` **按 canonical 语言动态 import + Promise cache**（显式 import map，禁止拼接任意 import 字符串；失败清 cache 可重试；未知/失败按纯文本）；加载中保持可编辑纯文本并保留 theme/syntax 扩展，切换文件用 request sequence 丢弃旧 Promise（language 与已装载 language 不一致时派生 extension=null，避免 effect 同步 setState）。bundle 合同暴露 `editorEntryLoaded`（WorkbenchCodeEditor lazy chunk gzip）；N7 实测 editor-entry ≈ 33.9 KiB gzip（baseline ratchet 仅下降不抬高），相对静态语言包时代至少下降 20%。
- **WorkbenchSessionSearch**: `src/components/domain/WorkbenchSessionSearch/`（`WorkbenchSessionSearch.tsx` + `.module.css` + `index.ts` + `.test.tsx`）是 Workbench 终端页的 Claude Code session 搜索 Command Palette。Props：`open` / `onClose` / `projectId` / `worktreeId` / `offline`（远端离线时禁用搜索输入并显示离线提示）/ `worktreeName` / `onResumed(sessionId)`（resume 成功后回调，父组件刷新 sessions 并切到新 window）。视觉是 v3 Command Palette 形态：共享 `Dialog` 提供 portal/backdrop/focus trap/列表 `Esc` 关闭，surface class 保持偏上 palette（约 12vh）与 opacity/translateY/scale 入场动画。交互：搜索 debounce 300ms、键盘导航 `↑↓` 移动高亮项、`⏎` 进 preview；列表 `Esc`/遮罩关闭由 Dialog 负责，preview 内 `Esc` 返回列表（`closeOnEscape={!previewHit}`）；preview 面板占据 palette 下半部分，展示该 session 最近 20 条对话（user/assistant 交替 + role 标签 + 时间）与 cwd/gitBranch/消息数/首末时间，底部「在新窗口 resume」主按钮（terracotta accent）+ 「取消」ghost 按钮。命中关键词在结果项内用 `<mark>` 包裹（`accent-soft` 背景 + `accent` 文字）。⌘K / Ctrl+K 全局触发：由 Workbench 页 `useEffect` 监听，仅在 `workspaceView === 'terminal'` 且非 input 聚焦时触发 `setOpen(true)`，避免与 xterm 输入冲突；入口按钮接入 `Workbench.tsx` 的 `WorkbenchWorkspaceNav` actions slot。API 走 `workbenchApi.claudeSessions.{search,preview,resume}`（封装 `search_claude_sessions` / `get_claude_session_preview` / `resume_claude_session`）；**search 返回有界 `SessionSearchResult{items,truncated,diagnostics}`**（`invokeDecoded` + `sessionSearchResultDecoder` fail-closed），组件渲染 `result.items`，当 `truncated===true` 或 `diagnostics.status==='truncated'` 时在结果列表上方展示非阻塞截断横幅（可附 max_files 等 reason 短标签），`status==='unavailable'` 时展示对端缺诊断提示（unavailable 优先）；截断**不**当硬错误，仍展示已有 items。resume 成功后调父级 `onResumed` 触发 `loadSessions` + `focusSession` 切到新 window。空态（无 session）、错误态（扫描失败，复用 `displayErrorMessage` + 重试）、远端离线态三种状态各自有专门 UI。所有颜色/间距/圆角/阴影取自 `tokens.css`，浅色/深色自动适配。
- **Workbench Merge UI**: Git 历史 Merge 按钮只要求 active worktree 非主且无其他 worktree 操作；不要用可能过期的 `status.clean` 禁用。点击后由后端 `merge_workbench_worktree` 实时检查源 worktree dirty 状态，并通过 `workbench:merge-progress` 推送带 `projectId` 的 `checkSource/closeSessions/mergeMain/resolveConflicts/cleanup` 阶段；前端只接收当前项目事件，在 Git 历史工具条下展示阶段条，命令结束后刷新 worktrees、sessions、Git history，并移除已关闭 worktree 的 terminal buffer。成功完成的阶段条只短暂展示后自动隐藏，失败阶段保留给用户查看错误。
- **Workbench 终端性能约定**: 终端输出缓存必须用 session 级外部 store（`createWorkbenchTerminalBufferStore(options?)`）。**桌面** `WorkbenchTerminalPane` 经 `useWorkbenchTerminalBufferStore()` + `createTerminalLiveWriter` 做 imperative live 订阅：先 `subscribeLive/subscribeReset` 再 `getSnapshot`，按 `generation/appendId` 去重后直接 `xterm.write`（单 in-flight；write 背压期间把后续 delta **有界合并**为单一 next-buffer 字符串 + 最后 cursor；`pendingChunk` 硬上限对齐 **200k UTF-16**，超限 → `needsSnapshot` 在当前 write 完成后 clear+replay 最新 snapshot；**禁止**无界 pending 数组/字符串增长）；Gap/reset 若写 in-flight，必须等当前 write 回调结束后再 clear（串行化），避免旧字节在 clear 后 paint。**移动端**复用同一 live writer，但用 `resetStrategy='preserveScrollback'`：首个 HTTP replay 建 baseline 后按 exact delta 直写；destructive reset 只提交新 generation cursor，不 clear 已写历史。**禁止**把任何 xterm live 输出放进 React effect / rAF / full-buffer KMP 热路径；`useWorkbenchTerminalBuffer(sessionId)` / `useSyncExternalStore` 仅服务非 xterm React 消费者。store 按 session 维护 chunk ring + `headIndex` + **`lastSeq`**（trim 完整头 chunk 只推进 head，禁止热路径 `Array.shift`；`headIndex>=1024` 且废弃前缀≥一半才 slice compact），超过 **200,000 UTF-16 code units** 时裁剪；`getSnapshot` 惰性 join 并缓存，`getBuffer` 委托 `getSnapshot().buffer`；`getLastSeq` 读 cutover。`append(sessionId, chunk, seq?)`：若提供 `seq` 且 `seq<=lastSeq` 则 no-op；否则 push/trim → `appendId+=1` → 推进 lastSeq → 同步 live delta → schedule React revision。多个 append 经 `TerminalFrameScheduler`（默认 rAF，测试可注入）合并 revision；**空 chunk no-op**；schedule 兼容同步 scheduler。`reset(sessionId, buffer='', lastSeq=0)` / `remove` 递增 generation、同步 notify reset listeners、立即 bump revision；resync/baseline 经 pure helper `applyTerminalBaselineCutover` 先 `reset` 再 re-append held 中 `seq > lastSeq` 的 live。对外 API：`getSnapshot/getBuffer/getRevision/getLastSeq/subscribe/subscribeLive/subscribeReset/append/reset/remove` + `applyTerminalBaselineCutover`。桌面 Tauri event 缓存与移动端 HTTP replay 分离：桌面 `workbenchApi.sessions.replay` invoke + 移动端 `/api/mobile/workbench/sessions/replay` 均返回 `WorkbenchSessionReplay{sessionId,buffer,truncated,lastSeq}`；移动 xterm 禁止重新引入 `planTerminalBufferWrite` 字符串补写（neutral helper 仅供 store 同 authority reset 规划）。**Task8 / P1 R1 合同**：input pump + generation/appendId/lastSeq handshake + live writer 有界背压 + StrictMode 可重建；Hooks 必须在 early return 前；隐藏 xterm 保持挂载（仅 data-hidden）。**L3 GUI latency: NOT VERIFIED**（release 本机 key-to-visible p95≤50ms/p99≤100ms 与 publish→listener p95≤20ms 未实测；自动化门禁不得声称手感达标）。**桌面终端输入泵**（`terminalInputPump.ts` + `useWorkbenchTerminalController.handleInput`）：per-session leading-edge FIFO；idle 首批立即 `writeInput`，in-flight 期间只拼接 pending，settle 后整批发送；每 session 最多 1 个 in-flight；**任一批次失败/未知后立刻 stop-on-failure：清空 pending、封锁 lane、`onWriteError` 投影 sessionError 并记入 `writeBlockedSessionIds`，`isWriteBlocked` 使 xterm `inputEnabled=false`，禁止静默键盘黑洞**；失败批次**永不自动重放**；`recoverSession`/`disposeSession` 抬 generation、清 pending、解 blocked（不重放）；**write fail 后自动 kick 只读 `sessions.list` status check**：仅当权威 list entry 的 status 严格为 `running` 时 `recoverSession` 解锁并同步 status/exitCode/exitedAt（不重放失败批次、不写输入）；list 含 id 但 `exited`/`disconnected` 或 check 失败则保持 blocked + sessionError，UI 可显式「重新检查」；**权威 `sessions.list`/`loadSessions` 成功**后 `recoverAllWriteBlockedSessions` **仅对 list 中 status===`running` 的 blocked id 解锁**（仍在 list 的 exited/disconnected 保持 blocked；消失 id 走 dispose+untrack）；TerminalArea `inputEnabled` 还须 `session.status === 'running'`；session close / closedWindow / clearBuffersForWorktree / loadSessions 列表替换中消失的 session / remoteWriteDisabled 上升沿 / controller unmount 调用 `disposeSession`；若 write 仍 pending 则保留 lane tombstone（`running=true`），re-enqueue 必须排队到旧 write settle 后才启动下一批，禁止同 session 双并发 drain；**StrictMode**：pump 在 mount effect 内创建，cleanup `dispose` 后清空 ref，下次 setup 重建。不触碰其它活动 session。终端面板 CSS 与 xterm theme 都必须读取 `--terminal-*` token，浅色/深色主题 token 必须分开，已挂载 xterm 在 `cp-theme-change` / `storage` 后同步 `terminal.options.theme`。**光标布局热路径**：`WorkbenchTerminalPane` 的 `emitCursorAnchor` 在 `onCursorAnchorChange` 未注册时必须在读取 DOM 前返回（零 `getBoundingClientRect`）；有回调时缓存 `TerminalCursorMetrics`（viewport 原点 + cell 宽高），`fit.fit()` / theme 变化后置空缓存，仅在 callback 存在时由 emit 重算。Prompt 优化 `handleCursorAnchorChange` 始终写 `cursorAnchorRef`，但浮层关闭时必须在 `terminalArea.getBoundingClientRect()` 前返回；只有浮层打开且位置变化时才更新浮层坐标，避免终端输出/滚动期间高频 cursor move 强制布局或卡住页面。
  - `preserveScrollback` 只适用于 reset reason=`snapshotReplace`（同 owner、无法安全字符串补写的短快照）；`authorityChange` / `explicitClear` 仍按默认策略串行 clear + replay 权威 snapshot。上文“destructive reset 不 clear”仅指前一种同 owner snapshot replace，不包含 owner cutover。
- **Mobile Workbench HTTP 事件**: `/mobile` 使用 `useWorkbenchHttpEvents` 读取 `/api/workbench/events` NDJSON；parser 只解析完整换行 JSON 行并保留 pending，hook 用 `TextDecoder(stream)` 处理 UTF-8 分包；server 每 15s 发 typed heartbeat `{"type":"heartbeat","sentAt":"<RFC3339>"}`，parser 在业务解码前识别。**Gap 一等帧**：`{type:"gap",payload:{ownerInstanceId,oldestAvailable,latest}}` 解析为 `kind:"gap"`（lag / ring truncate / owner switch 同形态）；**禁止**当 ignored 或继续 live。hook 使用 **lifecycle AbortController**（覆盖 hook 生命周期）+ **每连接 child controller**；维护 stream cursor（ownerInstanceId+sequence，从业务帧 envelope 推进）；重连 URL 附 `?afterOwnerInstanceId=&afterSequence=`（`buildWorkbenchEventsUrl`）。35s 无任何完整 data/heartbeat frame 时只 abort child 并在 lifecycle 仍活跃时重连；业务帧与 heartbeat 均重置 watchdog。**Gap 合同（R30/R32 M1 / R35 M1 / R41 M5 / R42 M2 / R43 M2）**：pause live → `sessions.list` 后对 `status===running` 调 `sessions.replay` → `store.reset(...)` 权威覆盖 → **R41 M5：inventory 成功后对全部 listed session（running/exited/disconnected）经 `onTerminalStatus`/`projectInventoryTerminalStatuses` 投影 terminalStatus（不得只依赖 live 帧；Gap 期间 status 迁移可能已丢）** → **成功则 cursor 始终 attach `{gap.ownerInstanceId, gap.latest}`（含 latest=0 的新 owner cutover）**，且当 `streamCursor.ownerInstanceId === gap.ownerInstanceId` 时清 `recoveryPending` 再重连；失败则保留 recovery cursor：**pre-gap 有 cursor 用 pre**，**首帧 Gap（pre=null）用 `{gap.owner, sequence:0}`**，置 `recoveryPending=true`；recovery 未完成时 **禁止 bare events URL**（`canOpenWorkbenchEventsRequest` + `buildWorkbenchEventsUrl` 必须带 after），**不得** brand-new consumer 掩盖 gap，也不得继续消费 laggy stream。terminalOutput 必须 `store.append(sessionId, chunk, seq, ownerInstanceId?)`（seq/owner 与桌面 authority 对齐），不要进入 React state；**R37 H2** `terminalResync` 经 `consumeWorkbenchHttpEvent` → `store.reset(sessionId, buffer, lastSeq, ownerInstanceId?)`（与桌面 Tauri resync 同语义）；live terminalStatus/mergeProgress/agentRuntime 仍走可选回调。**R42 M2 / R43 M2 Gap inventory（优先 mapped projects）** `resyncWorkbenchSessionsAfterGap` 默认注入 `listProjects` + `listActiveMappedProjects`（`POST /api/mobile/workbench/bridges/active-mapped-projects` → `{localProjectIds}`，对齐桌面 `bridges.active_mapped_projects`）+ 兼容 `listActiveBridgeDevices`：本机 `sessions.list()` 失败仍 throw；**mapped 已提供且成功**时仅 inventory 命中 `project.id` 的 remote（空数组跳过全部 remote；命中 id fail-closed）；**同设备活跃 P1 + 失效 P2 只 list P1**，P2 不得挡整次 resync；**mapped 非 unsupported 失败**（network/timeout/5xx 等）**fail-closed throw**，禁止 skip-offline soft-success，禁止推进 Gap cursor；**仅 404/unsupported**（`isMappedInventoryUnsupportedError`，对齐桌面 R42 M5）才回退 device 级（`active-devices`：deviceId 落在 active set 才 fail-closed；空 set 跳过全部 remote R40）；**mapped 未注入**时同样走 device 级；两者都未注入则 R38 skip-offline。禁止无关 offline/stale shortcut 永久阻塞本机 Gap recovery。纯 helper：`advanceWorkbenchHttpStreamCursor` / `resolveCursorAfterGap` / `buildWorkbenchEventsUrl` / `resolveRemoteProjectDeviceId` / `isMappedInventoryUnsupportedError` / `resyncWorkbenchSessionsAfterGap` / `projectInventoryTerminalStatuses` / `consumeWorkbenchHttpEvent`。
- **Mobile Workbench 状态 helper**: `src/mobile/mobileWorkbenchState.ts` 集中维护移动端 worktree、导航分组、连接态、viewport 与项目详情纯函数；`getMobileWorkbenchNavGroups(mode)`/`getMobileNavGroupIdForPanel(panel,mode)`/`resolveMobileNavMode`/`isMobileProjectBoundPanel` 双模式导航合同（global/project）；`computeMobileKeyboardInset`/`computeMobileViewportLayoutHints`/`computeMobileTerminalMinHeight` 供 shell 软键盘与横屏高度；`MobileConnectionState`/`MobileProjectDetailStatus`、`shouldSkipMobileProjectReload`（仅 ready 早退）、`markMobileConnectionOnline|Offline|Reconnecting`、`shouldRefreshMobilePanelOnReconnect`；`canSelectMobileProject` 允许 local/remote 并拒绝未知类型，`selectMobilePanelForProject` 在无项目时把项目绑定面板回落到 projects，有项目时保留请求面板；状态分类固定为真实 DTO 的 `conflicts > 0` 时 conflict、否则 dirty/clean，worktree switcher 在 local/remote 项目且非 busy 时打开，删除/合并等破坏性动作只允许非主 worktree 且非 busy。`mobilePanelState.ts` 另含 mutation 相位 `MobileMutationPhase` 与 `resolveMobileMutationPhase`/`pickMobileMutationOperationId`/`isMobileMutationActionLocked`。
- **Mobile Worktree quick switch**: `src/mobile/components/MobileWorktreeQuickSwitch.tsx` 是顶部 worktree pill 打开的 bottom sheet，**必须**用共享 `Dialog` 原语（portal/Escape/backdrop/focus trap），禁止手写 `role=dialog`/`aria-modal` 与本地 Escape 监听；只负责查看、刷新、切换和跳转完整 Worktrees 面板；Shell 状态栏里 project/session pill 保持静态文本，worktree pill 在 local/remote 项目且非 busy 时作为 `aria-haspopup="dialog"` 按钮打开该 sheet，不可用时禁用；不提供 merge/delete 等破坏性操作，状态文案必须复用 `getMobileWorktreeStatusKind`，文案位于 `workbench.mobile.worktreeQuickSwitch`。
- **Mobile Workbench 自动化面板**: `/mobile` shell 主导航里的项目级同级面板（入口紧跟项目面板之后，不属于 worktree quick switch）。**所有权拆分**：`useMobileAutomationController`（`src/mobile/controllers/useMobileAutomationController.ts`）持有全部 state/effects/handlers 与 `httpOrchestratorTransport` / runtime store 调用，并返回 shell + taskList + taskDetail + createDialog + outbox 的 typed props bundles；`MobileAutomationPanel.tsx` 只做壳层组合（header/refresh/create open/error/status/no-project + runtime strip + 四个视图），并 **re-export** `MobileAutomationPanelProps` / `MobileAutomationExecutionContext`（`MobileWorkbench` 仍从 panel 路径导入，lazy 路径不变）；纯展示子组件 `MobileAutomationTaskList` / `MobileAutomationTaskDetail` / `MobileAutomationCreateDialog` / `MobileAutomationOutbox` **禁止** `@/api/*` 与 transport 导入，只收 props 渲染 `MobileWorkbench.module.css`。本机 local 与远端 project shortcut 都可读取和创建项目级自动化任务。controller 经 `httpOrchestratorTransport.tasks.listViews/createView/completePrompt/listEvidence` 与 `getRuntimeSnapshot` 调用 `/api/orchestrator/task-views/list|create`、`/api/orchestrator/tasks/complete-prompt`、`/api/orchestrator/tasks/evidence` 以及 mobile-facing `POST /api/mobile/orchestrator/runtime-snapshot`（body `{projectId}`）；runtime snapshot 与 Tauri 共用 remote-aware 四态（local/live/offline/unsupported/unavailable），手机浏览器只打本机同源 route，不暴露 owning device P2P base URL。`src/mobile/mobileRuntimeSnapshotStore.ts` 维护与桌面分离的模块级 Map 成功缓存：按 projectId 隔离、stale request guard、仅 remote live 写入成功缓存、live→offline 保留最后 live 快照供显示、local 成功不写 live 缓存、cold offline 为空且中性“离线”文案、warm offline 才显示缓存提示、unsupported/unavailable 清空缓存；不写 localStorage/磁盘，缓存不能启用任务动作。display state 使用 `OwnedMobileRuntimeDisplayState` 记录 owning projectId；`begin/success/failure` 均 stamp 归属 id；controller 在 project 切换时同步 `emptyMobileRuntimeDisplayState(true,null,projectId)`，render 用 `selectMobileRuntimeDisplayForProject` 在 effect 前隔离 A→B 首帧旧 snapshot/status/cachedAt。runtime load 的 catch 必须经 `toRuntimeLoadError` 原样保留 `OrchestratorRuntimeTransportError`（仅非 Error reject 才包装），禁止 `new Error(getErrorMessage(reason))` 抹掉 network kind，否则 warm offline 缓存永远不可达。Panel 组合层在 snapshot 在场时 runtime 条须紧凑展示 owner `generatedAt`/`latestTickAt`/`recentEvents`（标题/消息）与 slots/running/retrying，local/live/offline-cached 均可见。task-view route 返回 local/remote/pendingRemote tagged union，真实任务按 `task.workflowState` 渲染为紧凑 grouped-list（TaskList），pendingRemote outbox 单独展示为待发送（Outbox），不参与 workflow 分组、selection 或 evidence；仅 failed 行通过 controller 的 `httpOrchestratorTransport.outbox.{retry,discard}` 提供 Retry/Discard（Discard 需 `window.confirm`），成功后 reload 列表；移动端自动化不实现横向拖拽。任务行展示 workflowState、runState、attemptPhase、lastRuntimeMessage、Claude session/transcript runtime 摘要和 local/remote 来源，缺失 runtime 显示 unknown；点击真实任务展开详情（TaskDetail），展示 title/goal/acceptance、workflow/run/attempt、runtime summary、blockedReason 和 evidence 时间线，并通过父级回调切换到现有 terminal 面板打开 worktreeId/sessionId 执行现场。创建任务入口必须是独立弹窗（CreateDialog 使用共享 `Dialog` 原语，busy 时 `closeOnEscape/closeOnBackdrop=false` 且 controller `handleCloseCreateDialog` early return；`initialFocusRef` 指向 short prompt；禁止手写 `role=dialog`/`aria-modal` 与本地 Escape 监听），不固定占用面板主文档流，弹窗支持手动填写标题/目标/验收标准，也支持输入简单 Prompt 后用 AI 自动完善三字段，AI 完善只填充表单不直接创建；提交按钮固定为“创建到 Backlog / 创建到 Todo / 创建并启动”，HTTP createView 必须携带非空 `clientRequestId` 并显式传 `createAction: 'backlog' | 'todo' | 'start'`，缺省为 `backlog`；**clientRequestId 在一次逻辑提交开始时生成，失败重试（表单内容未变）必须复用，成功/取消/表单变更后重新 mint**。completePrompt 总是传 `projectId`，本机项目可同时传 `project.path` 作为 `workingDirectory`，远端项目由本机 HTTP route 代理到 owning device 并使用远端项目路径；创建成功后清空弹窗表单并把返回 task view 合并进当前列表，刷新按钮重新读取当前项目任务与 runtime snapshot。A0 后 mobile 详情无 review diff 产品面；\*\*不\*\*暴露 Deliver/Rework。静态合同：`npm test -- MobileAutomationPanel`（transport/Dialog/runtime ownership 按 controller/CreateDialog/Panel/Outbox 分源扫描）。
- **Workbench worktree 创建**: `/workbench` 顶部“新建 worktree”先展开页面内表单，分支前缀从固定类型（`feature`/`fix`/`chore`/`docs`/`refactor`/`test`/`hotfix`）中选择，用户只输入后缀，确认后组合为 `<prefix>/<suffix>` 并调用 `create_workbench_worktree`；创建成功后立即用返回的 worktreeId 调 `create_workbench_session` 自动创建 1 个绑定该 worktree 的 terminal window（默认 1 个 pane），并切到该 worktree/window；如果 window 创建失败，不回滚已创建 worktree，只在终端错误区提示。分支前缀、默认前缀和 normalize/compose helper 统一从 `src/lib/workbenchWorktreeBranches.ts` 维护，`src/pages/Workbench/workbenchWorktrees.ts` 仅兼容 re-export；不要使用 `window.prompt` 等 WebView 阻塞弹窗，否则桌面环境可能没有可见反馈。
- **Workbench 页面控制器边界**: `src/pages/Workbench/Workbench.tsx` 是组合层与调度层，只持有跨域共享状态（`activeWorktreeId` / `workspaceView` / `automationConsoleOpen` / `inspectorTab` / DOM refs）和跨域 deferred effect（项目/worktree 切换时清场 + 拉取 worktrees/sessions/files/git、Git 历史 tab 加载、createWorktree 表单 focus），不实现任何域的具体 API 调用或事件订阅。会话运行时长由叶子 `SessionRuntimeText` 自持 1 Hz 时钟（`useSyncExternalStore`：document 可见性 + 共享 1 Hz 外部 store；仅 running + surface visible + document visible 时订阅 tick；`visible` 由 `!terminalFullscreen` 等既有状态派生），Workbench 根**禁止**持有 `runtimeNow` 或页面级 1s interval；五次 tick 不得抬升 Workbench 根渲染计数（`WorkbenchRenderBudget.test.tsx`）。页面必须满足 **1200 行总量上限**（`wc -l web/src/pages/Workbench/Workbench.tsx`，目前 ≤1200），超出即视为页面偷偷吞并了域逻辑，必须把新逻辑迁回对应 controller。各域逻辑全部下沉到 `src/pages/Workbench/controllers/` 下 7 个独立 controller hook，每个 controller 只暴露窄 `Params`（输入 + 回调）和窄 `Result`（权威 state + 操作函数），通过 barrel `controllers/index.ts` 统一 re-export：
    - **Workspace safe restore 窄 hook（非第 8 controller）** `useWorkspaceSafeRestore`：挂载在 Workbench 组合层（**须在 project/worktree 清场 effect 之前**，暴露 `suppressContextResetRef`），负责 `desktop:auto` layout autosave（500ms debounce + revision CAS，**完整 inspectorTab 枚举 + 真实 browserTargetUrl**）、启动 preflight→server apply（失败不静默 UI 选择）→post-apply actions UI bridge 与 partial `WorkspaceRestoreNotice`、命名 snapshot 同样走 UI bridge；**autosave 默认 pause**，首次 restore 完成或确认零项目后才 resume，apply 窗口内禁止 flush，避免半成品 selection 写脏 `layoutRevision` 导致 `workspace_layout_revision_changed`；apply 整单失败 reason 用 `classifyLayoutApplyError`（`layoutRevisionChanged` / `layoutMissing` / `applyFailed`），禁止只写死 `applyFailed`；仅含 `browserSkippedForNonBrowserView` / `browserNotRequested` / `worktreeNotRequested` / `sessionNotRequested` 的 notice 用 `isTransientRestoreNotice` 判定，弹出后 `TRANSIENT_RESTORE_NOTICE_MS` 自动关闭，真实 skip 仍钉住；apply 窗口内 project/worktree effect 只 load 不清 worktree/不强制 terminal；**不得**成为第 8 个业务 controller，不得保存 terminal 字节/文件正文/命令。纯逻辑见 `workspaceLayout.ts` / `workspaceRestore.ts`。BrowserWorkspace 经 `onBrowserTargetUrlChange` 回写 target。
- **项目域** `useWorkbenchProjectController`：持有 `remoteOfflineProjectId` 单一权威状态与 `activeProjectIdRef`；对外暴露 `remoteProjectOffline` / `remoteWriteDisabled` / `isCurrentProject(id)`（异步回调 stale guard）/ `markRequestFailure(id,err)`（仅当前 active 远端项目离线文案命中才置 offline）/ `markRequestSuccess(id)`（仅当 id 等于已记录 offline projectId 才清除）/ `selectProjectFromDeepLink(id)`；注册 activeProjectId 变化的 `queueMicrotask` 重置副作用，保留原 Workbench 的重置时序。不复制 worktree/session/file 状态。 另持有「继续工作」`launchSummary` 五 section 资源态与 `refreshLaunchSummary`：仅当 `projects.length > 0 && !activeProjectId` 时 fetch `get_workbench_launch_summary`（可见 ≤15s 轮询，离开模式 abort/作废 sequence）；整次失败 mark ready section `stale` 保留缓存；单 section error 不拖垮其它 section。零项目与 active 项目路径不请求。
  - **终端域** `useWorkbenchTerminalController`：持有 sessions / activeSessionId / sessionNameDraft / sessionBusy / sessionError / terminalFullscreen / terminalResizeRequestKey 单一权威状态；维护 activeProjectIdRef / activeWorktreeIdRef / knownSessionIdsRef / lastLocalFocusAtRef / **`terminalInputPumpRef`（mount effect 创建 + cleanup dispose/清空；StrictMode 可重建）**；注册 active session focus 同步、tmux focus 700ms 轮询、`workbench:terminal-status` 事件订阅、scopedSessions/activeSession.name defer effect；暴露 `WorkbenchTerminalBridge`（loadSessions / focusSession / createSessionForWorktree / clearBuffersForWorktree）和渲染数据/动作。**终端字节内容绝不进入 controller React state**，仍由 `WorkbenchTerminalBuffersProvider` 外部 store 持有；controller 只持有 session 元数据并通过注入的 `resetBuffer` / `removeBuffer` 回调清外部 buffer；`handleInput` 走 per-session 有序输入泵（非每键直接 writeInput；写失败 stop-on-failure 投影 `sessionError`/`writeSession` 并 track `writeBlockedSessionIds`，暴露 `isWriteBlocked`/`hasWriteBlockedSessions`/`retryWriteBlockRecovery` 供 TerminalArea 禁用输入与 StatusMessage 重新检查；写失败后 auto 只读 `sessions.list` status check（同 session inflight 合并）**仅当 list entry status 严格 `running` 时 unlock**（同时 merge status/exitCode/exitedAt；exited/disconnected 保持 blocked），**绝不** auto-replay 失败批次），`remoteWriteDisabled` 不入队。**close 必须在 await 前捕获 source projectId**：只作废源项目 list seq；若用户已切到其它项目，不得改当前 sessions/buffer/stats/error（A 关闭不得污染 B）；成功 close / closedWindow / worktree buffer clear / list 替换消失 session 时 `disposeSession` 丢 pending 并 untrack blocked；**`loadSessions` 成功 list 后仅对 status===`running` 的 blocked session `recoverSession` 解锁**（list 中仍在的 exited/disconnected 保持 blocked；不重放失败批次；与 auto status check 兼容）。`sessionListRequestSeqRef` + `isLatestRequest` 丢弃过期 `sessions.list`，create/close/split/rename 成功后 `invalidateSessionListRequests`，防止慢 list 覆盖 mutation。
  - **Worktree/Git 域** `useWorkbenchWorktreeGitController`：持有 worktrees / worktreeBusy / worktreeError / createWorktree 表单草稿 / gitCommits / gitHistoryLoading / gitHistoryError / mergeProgressWorktreeId / mergeStages 单一权威状态（`activeWorktreeId` 仍由页面持有，controller 接收为输入并透传 setter）；维护 merge 阶段自动隐藏定时器 ref；注册 `workbench:merge-progress` 事件订阅（按当前 project/worktree 过滤）；暴露 loadWorktrees / loadGitHistory / create/commit/push/merge/remove 操作函数。**worktree 创建走 `terminalBridge.createSessionForWorktree`，remove/merge 后的 terminal buffer/session 清理走 `terminalBridge.loadSessions` / `clearBuffersForWorktree`**——controller 不直接调用 `workbenchApi.sessions.*`，绝不直接 mutate 终端 state；`worktreeListRequestSeqRef`/`gitHistoryRequestSeqRef` + `isLatestRequest` 丢弃过期 list/history，create/remove/commit/push/merge 成功后 invalidate，防止慢响应回写旧 worktree/git 状态。**Git mutation ledger**：create/commit/push/merge/remove 的 success/catch/finally 均用 `beginMutationOperation` + `isSettledCurrent` 守卫（create 含 `setActiveWorktreeId`/`focusSession`/busy clear，同 project 内 worktree 切换也会使旧 create 过期）。commit/push/merge/remove 经 `workbenchApi.worktrees.*` 传稳定 `clientOperationId`，返回 typed envelope `succeeded|unknown`；unknown 后持有 `unknownMutationLock` 作为 worktree 级**共享锁**（同 kind+context 复用同一 id，二次同 kind 点击只对账不 mint 新 id；sibling kind 禁止 Fresh claim，handler early-return 且仅同 kind 可 `clearUnknownMutationLockForKind`；`canCommit/canPush/canMerge/canRemove` 与 GitInspector/WorktreeBar 按钮均消费 lock，sibling 禁用）；对账优先 ledger 终态 `succeeded|failed`，否则 authority 矩阵（merge 填充 `sourceWorktreePresent` + `mainContainsSourceHead` via main listCommits）；禁止盲重放。
  - **文件工作区域** `useWorkbenchFileController`：持有 rootNodes / childrenByPath / expandedPaths / selectedPath / selectedInfo / fileLoadingPath / fileError / fileNotice / fileTabs / activeFileTabId / fileSaving / newEntryName / renameName 单一权威状态；维护 per-path / per-tab / per-project+worktree 的请求序列 ref（dirRequestSeq / openFileRequestSeq / saveRequestSeq / formatRequestSeq / sqlitePreviewRequestSeq）做 stale/order guard；暴露 loadDir / loadPathInfo / 节点选择/展开 / 文件 tab 打开/激活/关闭 / 内容变更 / 模式切换 / 保存 / 格式化 / SQLite 表选择 / HTML 资源加载 / 新建/重命名/删除/复制 路径操作；暴露 `WorkbenchFileBridge`：`resetForContext(projectId, worktreeId)` 清空文件域全部状态并使挂起请求 stale、`guardDirtyContextChange()` 检查 dirty tab 并按用户确认决定是否允许切换 context。**跨域共享状态（`workspaceView` / `automationConsoleOpen`）仍归 Workbench.tsx 所有**，controller 通过注入的 `requestWorkspaceView` / `requestHideAutomationConsole` 回调表达切换意图，不直接 setState。
  - **自动化域** `useWorkbenchAutomationController`：持有 `deepLinkApplicationRef`（记录当前 search 已应用的 projectId/worktreeId/sessionId，防止重入）；注册 locationSearch 变化 effect（search 切换时把 applied 重置为「当前 search 但三段未应用」）和三段式 deep link 守卫 effect（project 段调 selectProjectFromDeepLink、worktree 段 setActiveWorktreeId、session 段 focusSession，每段必须等前一段对齐才应用下一段）；暴露 openAutomation / closeAutomation / openTaskWorkbench / applyAutomationDeepLink。**不持有 task fetching、worktree 列表、session 列表或终端字节内容**（OrchestratorPanel 自管任务列表）；`automationConsoleOpen` / `workspaceView` 跨域状态仍归页面。
  - **Prompt 优化浮层域** `useWorkbenchPromptOptimizerController`：持有 promptPanelOpen / promptInput / promptOptimizing / promptOptimizerHotkey / promptOptimizerFillLanguage / promptPanelPosition state 与 promptShortcutStateRef / promptPanelOpenRef；注册 configApi 加载 effect（mount 拉 hotkey/fillLanguage，失败回退默认）、打开后焦点回归 effect（rAF 聚焦 textarea）、Control 单键/组合快捷键 keydown+keyup 监听（capture 阶段，按 `reducePromptOptimizerShortcut` 状态机判定）；暴露 open/close/toggle/runPromptOptimization/handleCursorAnchorChange/handlePromptInputKeyDown 稳定函数。`handleCursorAnchorChange` early-gate：先写 `cursorAnchorRef`，浮层关闭或 anchor 为空时不读 `terminalAreaRef.getBoundingClientRect`。终端光标锚点 ref / prompt textarea ref 仍由页面持有（DOM ref 跨域共享），controller 通过注入 ref 读写。
  - **Session 搜索浮层域** `useWorkbenchSessionSearchController`：只持有 `sessionSearchOpen` state；注册 ⌘K/Ctrl+K keydown 监听（仅 `workspaceView === 'terminal'` 时生效，modifier 组合与 repeat 守卫与原 Workbench 一致）；暴露 openSessionSearch / closeSessionSearch / handleResumed（resume 成功后刷新 sessions + focus 新 session + 关闭浮层）。**搜索结果数据（query/hits/preview/debounce）仍归 `WorkbenchSessionSearch` 组件所有**，controller 绝不复制或代理。
  - **跨域协作只走 bridge，不允许直接互调**：worktree/git 域需要操作终端 session/buffer 时通过注入的 `WorkbenchTerminalBridge`（不直接 import `workbenchApi.sessions`），文件域/自动化域需要切换工作区视图时通过注入的 `requestWorkspaceView` 回调（不直接 setState 页面共享状态），prompt/session-search 域需要刷新终端 sessions 时通过注入的 `loadSessions` / `focusSession`。页面 Workbench.tsx 自身**禁止出现** `workbenchApi.(sessions|files|worktrees|git)` 直接调用，也**禁止** `listen('workbench:(terminal-status|merge-progress)')` 事件订阅——这些只能在对应 controller 内部出现。验证命令：`rg -n "workbenchApi\.(sessions|files|worktrees|git)|listen\('workbench:(terminal-status|merge-progress)'" web/src/pages/Workbench/Workbench.tsx` 应无任何匹配。
  - **characterization 测试与回归命令**：6 个 characterization 测试 `Workbench{Project,Terminal,Files,WorktreeGit,Automation,Overlays}.characterization.test.tsx` 锁定页面级跨域可观测行为（项目切换的 loadWorktrees/loadSessions/resetFile 序列、终端 focus 同步、文件 stale guard、worktree create→session 编排、自动化控制台开/关与 deep link takeover、overlay 开/关）；每个 controller 另有独立 `use<Domain>Controller.test.tsx` 单元测试窄 API。完整回归：`cd web && npm test -- src/pages/Workbench && npm test -- src/hooks/workbenchTerminalBuffer.test.ts src/hooks/workbenchHttpEvents.test.ts && npm test && npm run build`，全部必须 exit 0 且无 leaked timer/listener、unhandled Promise 或 xterm dispose 错误（React Router future flag warning 是已知噪音，不算失败）。
  - **hooks-before-return 规则**：所有 controller hook 必须在 Workbench.tsx 的 early return 之前无条件调用，保持 React hooks 调用顺序稳定（项目规则 20，同 Health/HealthOverlay/ReminderToast）。controller 内部 effect 也遵守同一规则——early return 出现前不可有条件 hooks。
  - **已知架构 gap（API 注入，deferred）**：终端/worktree-git/文件三个 controller 当前直接 `import { workbenchApi } from '@/api/workbench'` 调用后端，而不是按 Plan 2 原始设计接受 `Pick<typeof workbenchApi, ...>` 或显式方法接口注入。这是 Plan 2 Codex 评审 Finding 5 标记的 MEDIUM 级别架构问题，**作为 deferred 留给 whole-program review**：现网控制器单元测试已通过 `vi.mock('@/api/workbench', ...)` 顶层 mock 注入 fake，测试可测性并未实际受损；强制改写成构造期注入会同时改动 3 个 controller、3 个 controller 测试文件、`Workbench.tsx` 组合层与 `workbenchTestHarness`，对当前稳定代码风险大于收益。下次完整重构时再统一引入显式 `WorkbenchApiScope` 接口并在 `Params` 中注入，使生产/测试共用同一份窄 API 契约。
- **路由**: `src/App.tsx` — React Router，`/` → Home，`/prompts` → Prompts，`/cc-history` → CcHistory，`/workbench` → Workbench，`/scratchpad` → Scratchpad，`/prompt-optimizer` → PromptOptimizer，`/agent-hub` → AgentHub，`/claude-md` → `/agent-hub`；`/claude-code` 仍由路由兼容层进入 Agent Hub，并立即规范化为 `agent=claude&tab=skill`（legacy `section/target` 不进入生产状态；Rust/P2P N/N+1 facade 保留），`/orchestrator` → redirect `/workbench`，`/ssh` → SSH，`/health` → Health 等。**全部页面路由 `React.lazy` 加载**（`lazyNamed` 把 named export 适配为 `{default: module.Name}`）；providers 与 `AppShell` 保持 eager。AppShell 内每个 route 由 `ShellRoute` 包裹 `RouteErrorBoundary(resetKey=pathname)` + `Suspense`；`/screenshot-overlay`、`/health-overlay`、`/welcome` 用 `IsolatedRoute` 独立 boundary，主路由错误不得白屏 overlay。DesignSystem 仅 `isDev` 分支 helper 动态 import。错误页文案 `common:routeError.*`；开发可显示 `error.message`，永不渲染 stack。契约测试：`npm test -- RouteErrorBoundary App.lazyRoutes`
- **侧栏导航**: `AppShell` 按 Explore/Work/Knowledge/System 四组渲染（`section aria-labelledby` + 非聚焦 group label；文案 `nav:groups.*`）。顺序固定为 Explore: Github热门(`/`)；Work: 待处理(`/attention`，`formatAttentionBadgeCount` badge，0/null 隐藏，100+→99+) → 文件传输 + `WorkbenchProjectRail`（分区标题“工作台项目 / Workbench projects”；点击项目或添加本机/远端项目后进入 `/workbench`，不占独立「工作台」主导航项）；Knowledge: Prompt库 → Claude历史 → 速记本 → Prompt优化 → Agent Hub；System: 健康提醒 + Provider 管理。设置入口固定在侧栏 footer（主题与手机按钮之间的齿轮 NavLink `/settings`），不在主导航重复。不新增 Discover/`/discover`，不恢复独立“工作台/自动化”主导航项。`Sidebar.content` 使用 `min-height:0; overflow-y:auto`，footer 留在 flex 流底部，短窗口（含 1280×720）不与内容重叠。`WorkbenchProjectRail` 负责加载/选择/移除项目文件夹，空态展示说明与“添加本机项目/选择局域网项目”CTA（复用 `chooseAndAddProject` 与远端选择器，不新增项目 API）；项目卡片展示设备名、`window/pane` 统计以及可读状态文案（当前/未选中），状态不只靠颜色，不再显示“进入工作台”；**LAN Fleet（A6）**：`useLanAgentFleet`（30s visible reconcile + 500ms event coalesce + requestSeq）提供 project 摘要；Rail 仅异常 badge（needsInput/failed→`/attention`）与 Agent 状态点/offline 文本，working 不红标，**不再**放 Fleet 二级入口。只读详情在 Settings `?tab=fleet`（`SettingsFleetPanel` + `WorkbenchFleetView` 导航-only）；旧 `/workbench/fleet` replace 到该深链。点击右上角 `+` 先经共享 `Dialog` 选择本机或局域网设备来源（居中模态，不再用侧栏绝对定位 popover），本机继续打开系统目录选择器，远端再经共享 `Dialog` 打开 `WorkbenchRemoteProjectPicker` 选择在线设备、远端根目录和目录后调用 `open_workbench_remote_project`；远端打开期间 `closeOnEscape/closeOnBackdrop` 关闭，busy 门闩保留；远端弹窗采用左侧在线设备/可浏览位置导航、右侧目录浏览、底部选择状态与动作的文件选择器结构，Header/Body/Footer 必须显式保留 `md` padding 避免内容贴边；远端打开按钮只在当前 pathInfo 匹配选中 device/path、目标为可读目录且无 pending/open 请求时启用；打开期间禁用设备/目录切换并阻止关闭弹窗，以避免 stale 结果；点击已有项目后选择当前项目并跳转 `/workbench`；项目状态和终端统计由 `WorkbenchProjectsProvider` 共享给侧栏和 Workbench 页面。`/` 仍映射 Home 页面。
- **Workbench 远端项目 UX**: 远端项目卡片显示 Remote/远端 badge 和设备名；远端项目不要求对端已在 Workbench 预先添加或授权。若后端返回“远端设备不在线”，Workbench 只把当前 remote project 标为离线，展示 inline notice，并暂停终端输入、分屏/关闭 pane、session 重命名/关闭、worktree 创建/commit/push/merge/remove、文件新建/保存/格式化/重命名/删除和 Prompt 优化等写操作；项目快捷方式仍可从侧栏移除，成功读取 sessions/worktrees/files/git/pathInfo 后清除离线状态。
- **品牌图标**: `src/assets/app-icon.png` 是应用内品牌图标（AppShell 侧栏与 Welcome 页共用），由根目录 `scripts/cc-partner-icon.png` 缩放生成；源 PNG 必须保留透明外圈，避免 Dock/托盘/侧栏出现多余白底；不要在组件里硬编码 `cc`/`CP` 文本 logo。Tauri 系统/打包图标仍由 `src-tauri/icons/` 提供。
- **区域截图选区页**: `src/pages/Screenshot/Overlay.tsx` — 独立于 AppShell/OnboardingGuard，路由 `/screenshot-overlay?display={i}`，由 Tauri 选区窗口（每屏一个透明置顶窗口）直接加载。**微信截图风格 + 三态状态机**（`mode: idle | selecting | editing`）：
  - **idle**：整屏半透明黑色遮罩；mousedown（左键）进 selecting 开始框选。
  - **selecting**：拖拽框选，四块遮罩挖洞 + 蓝色虚线边框（选区内清晰）；mouseup 有效选区（宽高≥10）进 editing。
  - **editing**：mouseup 后立即进入 editing 并渲染 `ScreenshotToolbar`（矩形/箭头工具、6 色板 `COLORS`、撤销/确认/取消）与外侧 outline 选区框；工具条和选区框先完成首帧绘制，再调 `get_region_snapshot` 抓**纯桌面选区** PNG 作 canvas 背景底图（选区框画在 crop 外侧，选区外遮罩不进入 crop，避免快照捕获前闪烁/空白帧）；快照加载后 canvas 用 `useAnnotationCanvas` hook（`src/pages/Screenshot/useAnnotationCanvas.ts`）重绘「快照底图 + 全部标注」。确认时 canvas.toDataURL 合成「桌面选区 + 标注」→ `save_clipboard_image` 写剪贴板（**所见即所得，Rust 不画标注**）；选区过小 / ESC / 右键 → cancel。窗口真透明（onMount 强制 html/body `background:transparent` 覆盖全局主题底色，否则白屏）。坐标用逻辑像素 + `window.devicePixelRatio` 传 Rust 换算物理像素。
  - **hooks 必须在 early return 之前**（项目规则 20）。详见 `src-tauri/CLAUDE.md` M6 节
- **健康提醒页**: `src/pages/Health/Health.tsx`（路由 `/health`，样式 `Health.module.css`）— 状态监控页：标题说明 + 状态概览 Card + 今日活跃指标网格 + **习惯统计 Card（`HabitStatsCard`：今日饮水次数/休息次数/总休息时长 + 近 7 天 sparkline + 手动「+1 杯」按钮）** + 活动统计 Card；头部「配置」按钮跳转 `/settings?tab=health`（**完整配置表单已迁移至设置页健康提醒 tab，由 `HealthPanel` 渲染，本页不再渲染配置表单**；`HealthPanel` 数字字段 min/max 仅作前端 UX 约束并对齐后端合法区间：工作窗口 1–480 分钟、休息 1–120 分钟、喝水间隔 5–1440 分钟、保留天数 1–3650；免打扰起止均非空且相等时展示「全天免打扰 / All-day do-not-disturb」说明；后端字段错误经 `error` prop 原样展示，后端仍为权威校验）。后端 daemon 每分钟采样键鼠活跃度推进工作/休息状态机，连续工作达阈值触发久坐提醒（后端 daemon 与状态机见 `src-tauri/CLAUDE.md`「健康提醒」节）。`healthApi`(`src/api/health.ts`)封装命令（`get_health_config`/`get_default_health_config`/`get_health_status`/`toggle_health_enabled`/`toggle_health_paused`/`snooze_reminder`/`skip_reminder`/`update_health_config`/`get_activity_stats`/`get_activity_detail`/`record_water`/`skip_water_reminder`/`snooze_water_reminder`/`close_health_overlay` + 习惯统计 4 命令 `add_water_manual`/`delete_water_record`/`record_rest_completed`/`get_habit_stats`）。页面并行取 status + stats + detail + config + habitStats（startOfDay 起，startOfDay 取**本地当日 0 点**秒级 ts：`new Date(); d.setHours(0,0,0,0); floor(getTime()/1000)`，非 UTC 0 点；后端 `get_habit_stats` 也用 `local_start_of_day_ts` 同口径），经 `useVisibilityPolling` **每 5s** 可见性感知刷新（hidden 暂停、visible 立即刷新、single-flight；失败保留已有 status/stats/detail）；**HealthOverlay 本地休息倒计时不属于网络轮询，不暂停**；开关 enabled / 暂停 paused 走**乐观更新 + 失败回滚**（await 前记 prev、await 失败回滚到 prev + console.error，避免本地状态与后端不一致）。状态概览用 `Pill` + `ProgressBar` 表达 phase、监测开关、暂停/贪睡、当前工作进度、活跃/休息指标。`HabitStatsCard`(`src/pages/Health/HabitStatsCard.tsx` + `.module.css` + `.test.ts`)用纯 div bar 渲染饮水(橙 `--accent`)/休息(绿 `--success`)两栏 7 柱 sparkline（今日柱高亮），「+1 杯」按钮带 500ms 节流防同秒主键冲突（`water_records` 已改为自增 id 避免同秒丢计数），底层 `add_water_manual` 返回新 id 供 `deleteWaterRecord(id)` 撤销；rest 事件由后端 `start_overlay_rest` 到点 task 在休息自然结束时 `insert_rest_record`（kind='rest'）写入 `rest_records`（多屏由后端统一收尾仅记一次），reminder 触发由后端 daemon 在 `should_remind` 分支写入（kind='reminder'，DND/贪睡静默也计数）；中途 ESC/关窗经 `close_health_overlay` 取消到点 task 不记录 rest。detail 经 `StatsChart`(`src/pages/Health/StatsChart.tsx` + `StatsChart.module.css`)用 recharts 渲染「app 使用时长 top8 横向柱状图」+「窗口标题使用时长 top8 横向柱状图」+「24 小时活跃分布纵向柱状图」，图表颜色、tooltip、空态和容器样式必须走 design token。hooks 全部在 early return 之前（项目规则 20）。i18n namespace `health`（相位/状态/按钮/图表标题/配置项文案 noData 文案 + 习惯统计 20 key: habitStatsTitle/todayWater/todayRest/cup/times/totalRestMinutes/reminderTimesToday/nextWaterIn/waterOverdue/addCup/viewHistory/habitFooter/today/Mon..Sun）。
- **Tauri 顶层事件监听**: `App.tsx` 顶层挂 `PermissionNeededListener`、`HealthReminderListener` 与 `BackendCloseChoiceListener`；注册 `@tauri-apps/api/event.listen` 前必须先通过 `canListenToTauriEvents()` 检测 `window.__TAURI_INTERNALS__.transformCallback`，普通 Vite/Playwright 浏览器环境缺少 Tauri internals 时跳过注册，避免调试路由白屏。`BackendCloseChoiceListener` 仅在 `getCurrentWindow().label === 'main'` 时注册窗口关闭和托盘 `backend:close-requested` 监听，避免截图/健康 overlay 辅助窗口关闭被 `preventDefault()` 拦截；主窗口关闭选择经共享 `Dialog` 呈现；两条路径（仅关闭 GUI / 前后端都关闭）都必须先 `await pendingWrites.flushAll()`（`src/lib/closeFlush.ts` 的 `flushPendingWritesThenClose`），flush/busy 期间禁止 Escape/backdrop 关闭；flush 失败保持对话框打开并展示 `common:backendClose.error`，不得静默 exit；flush 成功后仅关闭 GUI 调 `backendApi.exitGui()`，前后端都关闭才先调 `backendApi.stop()` 再 exit。
- **健康提醒系统通知监听**: `App.tsx` 顶层 `HealthReminderListener`（与 `PermissionNeededListener` 同层、AppShell 之外）— 后端触发久坐提醒时 emit `health:reminder`（载荷 `{workWindowSeconds}`），本组件 `listen('health:reminder', ...)` 收到后用 `@tauri-apps/plugin-notification` 的 `sendNotification` 弹原生系统通知（标题/正文走 i18n `health:reminderTitle`/`health:reminderBody`，随当前语言切换）。
- **全屏健康提醒遮罩页**: `src/pages/HealthOverlay.tsx`（路由 `/health-overlay?display={i}&type=reminder|water`，独立于 AppShell/OnboardingGuard，与 `/screenshot-overlay` 同层）— 健康监测启用后，久坐/喝水提醒触发时由 Rust `open_health_overlay` 每屏建一个透明置顶遮罩窗口直接加载本页，建窗时按提醒类型带 `type` query。窗口真透明，onMount 强制 html/body `background:transparent` 覆盖全局主题底色（否则白屏，与截图 Overlay 同理）。页面用半透明黑色蒙层盖整屏，按 `type` 渲染：
  - **type=reminder**（默认）：久坐提醒文案 + 推迟 5/10 分钟 / 跳过 / **开始休息** 按钮（推迟/跳过复用 `close(snoozeMin?)`，调 `snooze_reminder`/`skip_reminder`）；点击「开始休息」调 `healthApi.startRest()`（后端 `start_health_rest` 写权威 `end_ts` + emit `health:rest-started {endTs}` 广播给**所有屏**遮罩窗口），各窗口监听该事件 + onMount 经 `getStatus()` 读 `overlayRestEndTs` 兜底，基于同一 `end_ts` 每秒刷新 `computeRestLeft(endTs)` 显示倒计时（多屏同步；到 0 由后端 task 统一 `record_rest_completed`+关闭所有窗口，前端不主动 skip）；resting 态显示「休息中…」+ MM:SS 倒计时 + 「按 ESC 关闭」提示（无操作按钮）。
  - **type=water**：喝水提醒文案 + 「已饮水」(`record_water`) / 「跳过」(`skip_water_reminder`) / 「延迟 5 分钟」/「延迟 10 分钟」(`snooze_water_reminder {minutes}`) 按钮，每个动作 try/catch + closeOverlay。
  - **ESC**：`window.addEventListener('keydown')` 任意态（actions/resting）直接 `close_health_overlay`（后端取消进行中的休息 task，不调 skip/record 等业务命令）。
  - i18n 复用 `health` namespace（reminderTitle/reminderBody/snooze5/snooze10/skip/startRest/resting/escToClose/waterTitle/waterBody/drank/skipWater/snoozeWater5/snoozeWater10）；模块级子组件用 `TFunction<'health'>` 接收 `t`，内部调用省略 ns 前缀。业务命令失败仍强关遮罩避免困住用户。hooks 全部在 early return 之前（项目规则 20）。
- **应用内健康提醒 toast**（`src/pages/Health/ReminderToast.tsx` + `WaterToast.tsx`，共用样式 `HealthToast.module.css`）：常驻渲染于 `AppShell`（主窗口内，任意页面下都能弹），与系统通知/全屏遮罩互补，给用户一个应用内可直接操作的悬浮卡。`ReminderToast` listen `health:reminder` → 显示久坐提醒卡（推迟 5/10 分钟 `healthApi.snooze` / 跳过 `healthApi.skip`，操作后关闭）；`WaterToast` listen `health:water` → 显示喝水提醒卡（「已喝水」`healthApi.recordWater` 后关闭）。两组件 hooks（useTranslation/useState/useEffect）全部在 `if (!visible) return null` 之前（项目规则 20）。i18n 复用 `health` namespace（reminderTitle/reminderBody/snooze5/snooze10/skip/waterTitle/waterBody/drank）。
- **LAN 风险披露（App 级 gate）**: `LanDisclosureGate` + `useLanDisclosureStartup` 挂在 `App` 最外层，覆盖 Routes 与权限 onboarding；未确认前不进入产品壳。确认状态按 app flavor 分文件（release `gui-bootstrap.json` / dev `gui-bootstrap.dev.json`），互不影响。**旁路**：`/screenshot-overlay` 与 `/health-overlay` 为系统遮罩窗口，不依赖 LAN 后端，gate 在这些 pathname 上直接渲染 children（仍不绕过主壳路由）。状态机 `loading|required|starting|error|pass`；失败 fail-closed 可重试。文案展示本机地址候选、首选 TCP 62116、端口递增、mDNS UDP 5353、精确无身份校验风险；确认后 invoke `acknowledge_lan_disclosure_and_start_backend`。**不是** pairing/token/LAN 模式。Welcome skip / `cp-permission-onboarded` 不能绕过本 gate。E2E 默认 mock 为已确认；首启旅程见 `frontend-foundation.spec.ts`。
- **重置首次启动引导（Settings 常规）**: `SettingsGeneralPanel` danger 卡 + 确认 Dialog；controller 编排 `backendApi.resetOnboardingGates()`（清**当前 flavor** bootstrap：release=`gui-bootstrap.json` / dev=`gui-bootstrap.dev.json` + stop backend）→ 清当前 flavor 的 `cp-permission-onboarded[.dev]` 与 `cp-permission-skipped[.dev]` → best-effort `pendingWrites.flushAll` → `exitGui()`。**不**清 Prompt/速记本/Claude Code 内容/`config.json`/主题语言；**不能**撤销 macOS TCC（文案说明）。失败不 exit。顺序合同：`npm test -- onboardingReset`。
- **macOS 权限流程**: 产品权限交互只展示屏幕录制（区域截图与窗口标题）、辅助功能（健康键鼠采样）和通知；输入监控没有独立消费者，不在 Welcome/Settings 展示，也不参与完成条件，后端四态 DTO/探测接口仅保留协议兼容与诊断；全局快捷键无需 TCC。`OnboardingGuard` 与页面进入只做 `check_permissions` 并按需导航 `/welcome`，禁止自动 Request/Open Settings/Reopen。Welcome/Settings 共用 `usePermissions`（2s 可见性轮询、single-flight），暴露 `request(type)`、`openSettings(type)`、`refresh` 等；侧栏徽标只导航 Welcome，不批量请求。Request 与 Open Settings 是两条独立命令，禁止同一次点击双开；无私有 TCC、`tccutil reset`、pending marker、运行时重签、自动 cold relaunch。系统设置返回后的屏幕录制/辅助功能 sticky 权限由 `welcomePermissionFlow` 多轮纯查询同步，必要时显示显式“重新打开应用”，仅该按钮调用 `relaunch_for_permissions`。完成/跳过标记仍按 app flavor 隔离；三项展示权限均 granted 才自动放行，任一未授权仍不阻塞“暂时跳过”。
- **速记本 autosave**: `ScratchpadAutosaveProvider` 常驻 AppShell（包在 AttentionProvider 内），队列实现 `src/hooks/scratchpadAutosave.ts`，Context `scratchpadAutosaveContext.ts`；Provider 把 `queue.flushAll` 登记到 `src/lib/pendingWrites` 单例。`Scratchpad` 页用 `queue.schedule/flushPage` 替代页面本地 timer；切页/创建/删除/同步前 await `flushPage`；路由 unmount 与 `pagehide` best-effort `flushAll`（queue 本身不卸载）；失败保留 pending 并显示重试。
- **Transfer 桌面旅程**（N5 recovery）：`src/pages/Transfer` 只走原生路径：`pickTransferFile`（plugin-dialog）与 `subscribeTransferFileDrops`（webview drop.paths）；选中态存 `{path,name}`，UI 仅展示 basename，完整路径作不透明 UTF-8 传 `transferApi.send(deviceId, filePath, clientOperationId)`；多文件 drop 只取首路径并 i18n 提示；`handleSendClick` 用 `sendingRef` 同步门闩防双击（与 cancel 的 `cancellingIdsRef` 同模式），按 `deviceId+path` mint/复用稳定 `clientOperationId` 后 `send`，再 `runTasksNow({force:true})`；lost ACK/uncertain 保留 intent 并 `getOperation` 对账，禁止 mint 新 id 盲双发。**动作矩阵**（`TransferItem` 可选 callback，无回调不渲染；`transferHistory.ts` 纯判定）：pending/transferring/finalizing → `onCancel`；failed+resumable（Send + progress/resume metadata + **devices 列表 peer capabilities 精确含 `transfer.resume.v1`**，缺省 fail-closed）→「继续传输」`transferApi.resume(taskId, clientOperationId)`；failed+retryable 无 resume metadata、旧 peer 无能力或 cancelled →「重新传输」`retry`（`isTransferResumable(task, peerSupportsResume)`）；receive+completed（same-device GUI only）→ Open/Reveal（`transferApi.open/reveal`：`prepare_transfer_open` → `plugin-opener`；P2P/mobile unsupported）；Send completed 不暴露 Open/Reveal。**clientOperationId**：首次 send 与 retry/resume 每个 user intent mint 一次，pending/unknown 期间复用；**不是** HTTP/X-CC-Request-Id。**同 logicalTransferId 互斥**（`isLogicalTransferRecoveryLocked`）：任一 sibling attempt 为 pending/transferring/活跃 phase 或 reconciling 时，旧 failed 行禁用 resume/retry，禁止另 mint 新 op 并发发送；锁持有到终态。transport timeout/network 进 reconciling，展示「正在确认结果」并 `getOperation` 对账，禁止 blind retry。历史分区 active / needs-attention / recent-completed，空组省略；phase/failure message + 可复制稳定 code。刷新失败按 `faultRecovery` 的 cachePolicy 决定 keepStale 或 clear。L1：`E2E-TRANSFER-001` / `transfer.spec.ts`（send/cancel 基线；recovery 单元见 `Transfer.test.tsx` / `TransferItem.test.tsx` / `transferHistory.test.ts` / `api/transfer.test.ts`）。L2：`L2-TRANSFER-RECOVERY-001`（`transfer_recovery_smoke`）。1 GiB dual-host resume：`L3-DUAL-HOST-LAN-001` **NOT VERIFIED**。设备 5s / 任务 3s 走 `useVisibilityPolling`。**实时进度**：Tauri 环境（`canListenToTauriEvents`）挂载时 `listen('transfer:progress'|completed|failed|cancelled)`，经 `src/lib/transferProgress.ts` 的 `mergeTransferProgressEvent` / `mergeTransferStatusEvent` fail-closed 解码合并（损坏 payload / 未知 status 返回 null 跳过，日志仅 contract/path，禁止打印 payload）；卸载 unlisten；浏览器调试无 Tauri internals 时不注册。
- **Prompt mutation 事务**: `src/pages/Prompts/promptMutations.ts` 提供 apply/commit/rollback 纯函数；页面乐观更新，API reject 回滚列表、create/update 恢复草稿，并展示 `prompt-mutation-error` + 原地重试。
- **Prompt/Scratchpad 版本历史**: `ContentVersion`（`lib/types/core`）对齐后端 content_versions 摘要；共享 `VersionHistoryDrawer` 展示 createdAt/sourceDevice/kind/preview，支持确认后 restore-as-new-version（推进本机 vector clock，不覆盖旧历史）与复制内容，不提供三方合并编辑器。后端保留策略：history 约 20 版本或 30 天（先达限），conflict 至少 30 天。冲突版本用非阻塞 `Pill tone="warn"` 标识（卡片标题旁/编辑区标题旁），不禁用编辑与保存。
- **Claude 历史项目归并与设备筛选**: 后端会把仓库子目录、当前 worktree 与可恢复身份的已删除 worktree 历史持久迁移到 Git 主项目路径；`CcHistory` 先按历史所属 `deviceId` 筛选，默认本机，项目列表与 `listPrompts(projectPath, search, deviceId)` 必须使用同一设备条件，且离线历史设备仍可选。项目选择键使用主路径，不依赖 Workbench 项目/worktree 当前是否登记。删除 Prompt 的乐观计数必须按当前选中主项目路径更新，并把该聚合路径保留到失败重试上下文，不能按旧 item.projectPath 误更新不存在的独立项目。
- **Devices draft / CcHistory 动作恢复**: `Devices.saveTarget` 返回 `Promise<boolean>`；行内编辑与手动添加仅在 `true` 时清除 draft/表单，失败保留可编辑输入并展示 `sshError` +「重试」。`CcHistory` 转存 Prompt / 删除失败统一用 `StatusMessage tone="danger"`（`role=alert` 恰好一次）+ 条目快照重试；删除先快照再乐观移除，`remove` 失败回滚列表项/项目计数并提供重试；成功删除不暴露假 Undo（无后端 restore 合同）；成功 toast 用 `StatusMessage tone="success"`。契约：`npm test -- Devices.test.tsx CcHistory.test.tsx`。
- **Async feedback 可访问性合同**: 原子组件 `StatusMessage`（`web/src/components/primitives/StatusMessage`）统一异步反馈 live region：`tone=success|info|warn` → `role=status` + `aria-live=polite`；`tone=danger` → `role=alert` + `aria-live=assertive`；可选 `action` 插槽承载重试。已接线：ClaudeMd 保存/推送 toast、CcHistory 转存/删除失败与成功 toast、桌面 Workbench `sessionError`/`worktreeError`/`gitHistoryError`、MobileGitPanel error/reconciling。busy 按钮保持稳定 accessible name（`aria-label`/`loading` 不改动作名）。`TagInput` props 为 accessible-name XOR：恰好 `ariaLabel` 或 `ariaLabelledBy` 其一，Prompts 编辑器传 `t('prompts:tagInputAriaLabel')`，placeholder 不充当名称。契约：`npm test -- StatusMessage TagInput ClaudeMd CcHistory MobileGitPanel`。
- **Operation context / Workbench Git mutation**: pure helpers 在 `src/lib/asyncState/`（`saveAttempt` / `operationContext` / `mutationOutcome` 含 `WorkbenchMutationUnknownError` typed code）与 `workbenchMutationReconciliation`（ledger 终态优先 + `buildMergeRemoveAuthority`）；桌面 create 与四类 mutation 全量 `isSettledCurrent`；桌面 unknown 保留 `unknownMutationLock` **共享锁**（sibling kind 禁用 + 仅同 kind 清锁 + can* UI 接线）；移动 `MobileGitPanel` 的 `mutationPhase`/op-id 按 `{projectId,worktreeId}` 重置；`MobileWorktreePanel` 的 unknown **按操作 worktree id 作用域**保留（仅 `project?.id` 变化清空，active 切换不解锁），reconcile/retry/merge/remove 每个 await 后用 settled key（projectId + op worktreeId + sequence）守卫写入；unknown 判定只看 typed code/envelope（禁止中文子串），StatusMessage 提供 same-id「重新核对」；pending/notFound/ambiguous 保持 unknown 并锁盲重放。
- **自定义 Hook**: `src/hooks/` — `useTheme`（浅/深主题切换与跨组件同步）、`useAppVersion`（应用版本号，统一经 `invoke('get_version')` 获取，**禁止前端硬编码版本号**，唯一权威来源是 `tauri.conf.json` 的 `version`）、`useLanguage`（中英文切换，复刻 useTheme 的 localStorage + 自定义事件同步范式）、`usePermissions`（macOS 权限状态可见性轮询 + 显式逐项动作：基于 `useVisibilityPolling` 2s；后端四字段经 `configApi.permissions()` 查询，但产品只展示/要求 screenCapture、accessibility、notification，inputMonitoring 仅协议兼容；`request(type)` 与 `openSettings(type)` 分别走独立 IPC；通知权威为 UNUserNotificationCenter，非 plugin stub；`lib/notification.ts` 的 `checkNotificationGranted` 同样委托 `check_permissions`；暴露 `loading/refreshing/error/requesting/request/openSettings/refresh/allRequiredGranted`；首轮失败结束 loading，刷新失败保留 stale，同 type 动作去重；侧栏仅导航 Welcome，不提供批量 Request；三项展示权限均需授权才完成引导；导出 `PERMISSION_ONBOARDED_KEY`）、`useVisibilityPolling`（可见性 + single-flight 网络轮询：默认立即执行、hidden 停 interval、visible 立即刷新；普通 `runNow` 复用 in-flight Promise，mutation 后 `runNow({ force: true })` 在当前 in-flight 结束后强制再跑一轮；Transfer 任务 3s / 设备列表 5s / Health 5s / Permissions 2s 使用本 hook；HealthOverlay 本地倒计时不迁移）、`WorkbenchDependencyProvider` + `useWorkbenchDependency`（全局共享 tmux 依赖检测/安装/轮询状态，Provider 包在 `WorkbenchProjectsProvider` 外层）、`WorkbenchProjectsProvider` + `useWorkbenchProjects`（工作台项目列表、当前项目和 terminal window/pane 统计共享状态；Provider 文件只导出组件，Context/hook 放在 `workbenchProjectsContext.ts` 以满足 Fast Refresh；当前项目 id 持久化在 localStorage，会话 tab 恢复由后端 `workbench_sessions` 表负责）、`WorkbenchTerminalBuffersProvider` + `useWorkbenchTerminalBuffers`（常驻监听 `workbench:terminal-output` 并按 session 缓存输出，Provider 包在 AppShell 外层；Workbench 路由切出再切回时只能消费该缓存 replay，不能把 terminal output 监听和 buffer 放回页面内部，否则 Claude Code/tmux TUI 会丢屏幕态并错位）、`AttentionProvider` + `useAttention`（`useAttention.tsx` + `attentionContext.ts` + `attentionState.ts` + `attentionInvalidation.ts`：共享 Inbox 快照状态机；注入 `loadSnapshot` 供桌面 Tauri / 移动 HTTP 共用；requestId 防旧响应覆盖；**single-flight/coalescing**：in-flight 时区分 **poll-pending**（10s 轮询）与 **force-pending**（挂载/focus/可见/手动/invalidation），禁止定时器饿死慢聚合；**`ATTENTION_LOAD_TIMEOUT_MS`（35s）** 包裹 loadSnapshot；**任何 load 失败**（硬超时或普通 reject，含移动端 30s HTTP 超时）都必须**丢弃 poll-pending 且不得立即链式再起请求**，仅成功后才消费合并的 poll-pending；硬超时额外**暂停自动轮询**，仅 force 路径（手动 refresh/focus/invalidation/可见恢复）可恢复；普通失败保留错误态，等待下个 interval tick 或显式 force；有 snapshot 时刷新失败只标 stale 保留列表与 badge；初次失败 snapshot=null 不显示虚假数字；仅 document 可见时每 10s 轮询，hidden 暂停；hooks 全在 early return 前）
- **i18n**: `src/i18n/` — react-i18next 多 namespace（en/zh）；语言检测 localStorage(`cp-lang`) > `navigator.language` > en；切换器在 Sidebar 底部。**禁止在组件里硬编码用户可见中/英文字面量**，一律走 `src/i18n/locales/{en,zh}/<ns>.json` + `t('<ns>:<key>')`。详见下方「i18n 国际化」。

> 迁移到 Tauri 后已移除 Vite `dynamicApiProxy` 插件与 `~/.cc-partner/backend.port` 机制——前端走 invoke，无需 HTTP 代理。

## 打包部署

- `npm run build` 输出到 `dist/`，由 Tauri 打包嵌入应用（`tauri.conf.json` 的 `frontendDist=../web/dist`）
- 生产构建：仓库根 `./web/node_modules/.bin/tauri build`（产出 dmg/nsis/appimage 等）

## i18n 国际化

- **库**: react-i18next + i18next，初始化在 `src/i18n/index.ts`（`declare module` 类型扩展，`t()` 的 key 编译期校验，拼错即 tsc 报错）
- **命名空间**: `common`（动作/状态枚举/方向，跨页共享）、`nav`、各页面一个（home/attention/prompts/ccHistory/transfer/scratchpad/promptOptimizer/claudeMd/welcome/settings/health/workbench/orchestrator）
- **写法约定**（i18next v26 类型硬要求）:
  - 组件内 `const { t } = useTranslation([用到的所有ns数组]);`，所有 `t('ns:key')` **带 ns 前缀**
  - 模块级 helper 接收 `t: TFunction<'ns'>`，内部调用**省略 ns 前缀**
  - 插值 `{{var}}`；英文复数 en `{key}` + `{key}_other`，zh 只 `{key}`
- **切换/持久化**: `useLanguage` hook（localStorage `cp-lang` + `cp-lang-change` 自定义事件 + `storage` 事件跨标签/多窗口同步）；首次按系统语言推断
- **术语保留英文**: Prompt / GitHub / KB·MB 等在 en/zh 资源都保留英文
- **DesignSystem 页**: dev-only，不纳入 i18n（保持英文）
- **AST 合同 / 对等**: `npm run check:i18n` 禁止生产 TSX 硬编码 letterful JSXText 与 title/aria-label/placeholder/alt 字面量；`npm test -- localeParity` 要求 zh/en namespace 与 leaf key（复数后缀归一化后）对等；新增 key 必须中英同提交
- **新增页面文案**: 在 `src/i18n/locales/{en,zh}/<页ns>.json` 加 key + 组件 `useTranslation(['<页ns>','common'])` + `t('<页ns>:key')`；改完 `npm run check:i18n && npm test -- localeParity && npm run build`（tsc 校验 key）

### Agent 使用统计 UI（Agent Metadata Ledger / A9）

- 用户文案统一为「Agent 使用统计 / Agent usage stats」（不再写 metadata history）。
- 类型/schema：`web/src/lib/types/agentLedger.ts`、`web/src/lib/schemas/agentLedger.ts`。
- API：`workbenchApi.agentLedger.{list,summarize,clear}`。
- **查看入口**：Workbench 终端工具栏 `AgentLedgerWorkbenchChrome` 按钮（`data-testid=agent-usage-stats-open`）→ `AgentLedgerDrawer` 二级抽屉（用量摘要 + 最近会话列表）；加载态由 `useWorkbenchProjectController` 拥有，views 不 import `@/api/*`。
- **发现性互链**：Drawer 底部「去设置清除」→ 关闭后 `navigate('/settings?tab=general')`（`data-testid=agent-usage-stats-open-settings`）；Settings 常规 helper 说明数据在工作台按钮查看、清除不影响终端/任务/Prompt/对话。
- Fleet：`WorkbenchFleetView` 展示 7d Agent activity（unsupported/unavailable 不显示 0 tokens）。
- Settings 常规 tab：一键清除使用统计 + Dialog 确认。
