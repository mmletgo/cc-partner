/**
 * RuntimeDiagnosticsCard barrel
 *
 * Business Logic（为什么需要这个入口）:
 *   页面与 Settings 依赖面板可按短路径导入运行诊断卡片。
 *
 * Code Logic（这个入口做什么）:
 *   re-export 组件。
 */

export { RuntimeDiagnosticsCard } from './RuntimeDiagnosticsCard';
